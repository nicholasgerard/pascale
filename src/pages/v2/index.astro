---
import Layout from '../../components/v2/Layout.astro';
---

<Layout
	title="Agissons Ensemble avec Pascale Richard — Élections consulaires 2026"
	description="Pascale Richard, conseillère des Français de l'étranger, candidate aux élections consulaires 2026 pour NY-NJ-CT-Bermuda."
	path="/v2/"
	currentPage="accueil"
>
	<!-- ═══ HERO ═══ -->
	<section class="hero">
		<div class="hero-inner">
			<p class="election-label">Élections consulaires 2026</p>
			<div class="hero-photo-wrapper">
				<img src="/assets/images/pascale.jpg" alt="Pascale Richard" class="hero-photo" />
			</div>
			<h1>Agissons Ensemble<br /><span class="avec">avec Pascale Richard</span></h1>
			<div class="geo">
				<span>New York</span>
				<span class="dot"></span>
				<span>New Jersey</span>
				<span class="dot"></span>
				<span>Connecticut</span>
				<span class="dot"></span>
				<span>Bermuda</span>
			</div>
			<p class="hero-intro">
				Conseillère des Français de l'étranger, engagée depuis plus de 25 ans au service de notre communauté française en Amérique du Nord.
			</p>
			<div class="hero-buttons">
				<a href="/v2/programme" class="btn btn-primary">Notre programme</a>
				<a href="/v2/la-liste" class="btn btn-ghost-light">Découvrir la liste</a>
			</div>
		</div>
	</section>

	<!-- ═══ KEY FACTS ═══ -->
	<section class="facts-bar">
		<div class="container">
			<ul class="facts-list">
				<li>
					<strong>25+</strong>
					<span>ans à New York</span>
				</li>
				<li>
					<strong>Engagée</strong>
					<span>depuis 2016</span>
				</li>
				<li>
					<strong>Élue</strong>
					<span>en 2021</span>
				</li>
				<li>
					<strong>Suppléante</strong>
					<span>de R. Lescure</span>
				</li>
			</ul>
		</div>
	</section>

	<!-- ═══ ABOUT PASCALE ═══ -->
	<section class="about">
		<div class="container">
			<div class="about-grid">
				<div class="about-portrait">
					<img src="/assets/images/pascale.jpg" alt="Pascale Richard" />
				</div>
				<div class="about-content">
					<div class="section-header" style="justify-content: flex-start;">
						<p class="section-label">Qui suis-je</p>
						<span class="section-rule"></span>
					</div>
					<h2>Pascale Richard</h2>
					<p class="about-role">Conseillère des Français de l'étranger</p>
					<p class="about-bio">
						Installée à New York depuis plus de 25 ans, Pascale Richard a d'abord mené une carrière de journaliste en tant que rédactrice en chef de France Amérique, puis directrice des événements culturels au Lycée Français de New York.
					</p>
					<p class="about-bio">
						Engagée depuis 2016 avec le mouvement En Marche, elle a accompagné Roland Lescure dans sa campagne pour l'Amérique du Nord. Élue conseillère des Français de l'étranger en 2021, elle siège à l'Assemblée des Français de l'étranger, commission développement durable.
					</p>
					<a href="/v2/la-liste" class="btn btn-ghost" style="margin-top: 1.5rem;">Voir toute la liste</a>
				</div>
			</div>
		</div>
	</section>

	<!-- ═══ PRIORITIES OVERVIEW ═══ -->
	<section class="priorities-overview">
		<div class="container">
			<div class="section-header">
				<span class="section-rule"></span>
				<p class="section-label">Engagements</p>
				<span class="section-rule"></span>
			</div>
			<h2 class="section-title">Trois priorités pour notre communauté</h2>
			<div class="priority-grid">
				<div class="priority-card">
					<div class="card-icon">01</div>
					<h3>Éducation</h3>
					<p>Soutenir l'accès à l'éducation française et bilingue pour nos enfants, renforcer les liens avec les établissements scolaires et les bourses.</p>
				</div>
				<div class="priority-card">
					<div class="card-icon">02</div>
					<h3>Social</h3>
					<p>Accompagner les Français en difficulté aux États-Unis — personnes âgées isolées, familles touchées par la vie chère, accès aux droits sociaux.</p>
				</div>
				<div class="priority-card">
					<div class="card-icon">03</div>
					<h3>Santé</h3>
					<p>Faciliter l'accès aux soins pour les Français de l'étranger, améliorer la couverture santé et accompagner les démarches médicales transfrontalières.</p>
				</div>
			</div>
			<div style="text-align: center; margin-top: 2.5rem;">
				<a href="/v2/programme" class="btn btn-ghost">En savoir plus</a>
			</div>
		</div>
	</section>

	<!-- ═══ QUOTE BAND ═══ -->
	<section class="quote-band">
		<div class="container">
			<blockquote>
				« Il y a une résilience des gens qui n'attendent pas tout de leur gouvernement. C'est notre relation à l'État qui doit évoluer. »
			</blockquote>
			<cite>— Pascale Richard</cite>
		</div>
	</section>

	<!-- ═══ SOUTIENS PREVIEW ═══ -->
	<section class="soutiens-preview">
		<div class="container">
			<div class="section-header">
				<span class="section-rule"></span>
				<p class="section-label">Ils nous soutiennent</p>
				<span class="section-rule"></span>
			</div>
			<h2 class="section-title">Des soutiens de premier plan</h2>
			<div class="soutiens-logos">
				<div class="soutien-badge">Eléonore Caroit</div>
				<div class="soutien-badge">Olivier Cassegrain</div>
				<div class="soutien-badge">Roland Lescure</div>
				<div class="soutien-badge">Christopher Weissberg</div>
			</div>
			<div style="text-align: center; margin-top: 2rem;">
				<a href="/v2/soutiens" class="btn btn-ghost">Voir tous les soutiens</a>
			</div>
		</div>
	</section>

	<!-- ═══ CTA ═══ -->
	<section class="cta-section">
		<div class="container">
			<h2>Rejoignez le mouvement</h2>
			<p>Ensemble, construisons l'avenir de notre communauté française aux États-Unis.</p>
			<div class="cta-buttons">
				<a href="https://www.instagram.com/pascalevrichard" target="_blank" rel="noopener noreferrer" class="btn btn-primary">Suivez-nous sur Instagram</a>
				<a href="/v2/evenements" class="btn btn-ghost-light">Nos événements</a>
			</div>
		</div>
	</section>
</Layout>

<style>
	/* ─── HERO ─── */
	.hero {
		background: var(--navy-deep);
		color: var(--white);
		text-align: center;
		position: relative;
		overflow: hidden;
	}

	.hero::before {
		content: '';
		position: absolute;
		top: 0;
		left: 0;
		right: 0;
		bottom: 0;
		background: radial-gradient(ellipse at center top, rgba(0, 35, 149, 0.4) 0%, transparent 70%);
		pointer-events: none;
	}

	.hero-inner {
		max-width: 720px;
		margin: 0 auto;
		padding: 4rem 2rem 5rem;
		position: relative;
		z-index: 1;
	}

	.election-label {
		text-transform: uppercase;
		letter-spacing: 0.25em;
		font-size: 0.6875rem;
		font-weight: 700;
		color: rgba(255, 255, 255, 0.45);
		margin-bottom: 2rem;
	}

	.hero-photo-wrapper {
		margin-bottom: 2rem;
	}

	.hero-photo {
		width: 152px;
		height: 152px;
		border-radius: 50%;
		object-fit: cover;
		object-position: center top;
		border: 3px solid var(--white);
		box-shadow: 0 0 0 4px var(--red), 0 8px 32px rgba(0, 0, 0, 0.3);
	}

	.hero h1 {
		font-size: clamp(2rem, 5vw, 3.25rem);
		font-weight: 700;
		line-height: 1.15;
		margin-bottom: 1rem;
		letter-spacing: 0.04em;
		text-transform: uppercase;
	}

	.avec {
		font-size: 0.7em;
		font-weight: 400;
		letter-spacing: 0.08em;
		opacity: 0.85;
	}

	.geo {
		display: flex;
		justify-content: center;
		align-items: center;
		gap: 0.75rem;
		font-size: 0.8125rem;
		color: rgba(255, 255, 255, 0.4);
		text-transform: uppercase;
		letter-spacing: 0.15em;
		margin-bottom: 2rem;
	}

	.dot {
		width: 4px;
		height: 4px;
		border-radius: 50%;
		background: var(--red);
		opacity: 0.8;
	}

	.hero-intro {
		font-size: 1.0625rem;
		line-height: 1.75;
		color: rgba(255, 255, 255, 0.65);
		max-width: 520px;
		margin: 0 auto 2.5rem;
		font-style: italic;
	}

	.hero-buttons {
		display: flex;
		gap: 1rem;
		justify-content: center;
		flex-wrap: wrap;
	}

	/* ─── KEY FACTS BAR ─── */
	.facts-bar {
		background: var(--navy);
		padding: 0;
	}

	.facts-list {
		list-style: none;
		display: grid;
		grid-template-columns: repeat(4, 1fr);
		text-align: center;
	}

	.facts-list li {
		padding: 1.5rem 1rem;
		border-right: 1px solid rgba(255, 255, 255, 0.1);
		color: rgba(255, 255, 255, 0.6);
		font-size: 0.8125rem;
	}

	.facts-list li:last-child {
		border-right: none;
	}

	.facts-list strong {
		display: block;
		color: var(--white);
		font-size: 1.25rem;
		margin-bottom: 0.25rem;
		letter-spacing: 0.02em;
	}

	/* ─── ABOUT ─── */
	.about {
		padding: 5rem 0;
		background: var(--off-white);
	}

	.about-grid {
		display: grid;
		grid-template-columns: 280px 1fr;
		gap: 3.5rem;
		align-items: start;
	}

	.about-portrait img {
		width: 100%;
		aspect-ratio: 3 / 4;
		object-fit: cover;
		object-position: center top;
		border: 1px solid var(--border);
	}

	.about-content h2 {
		font-size: 2rem;
		color: var(--navy);
		margin-bottom: 0.375rem;
		font-weight: 700;
	}

	.about-role {
		font-size: 1rem;
		color: var(--red);
		font-style: italic;
		margin-bottom: 1.5rem;
		letter-spacing: 0.02em;
	}

	.about-bio {
		font-size: 1rem;
		line-height: 1.8;
		color: var(--muted);
		margin-bottom: 1rem;
	}

	/* ─── PRIORITIES OVERVIEW ─── */
	.priorities-overview {
		padding: 5rem 0;
		background: var(--white);
	}

	.priority-grid {
		display: grid;
		grid-template-columns: repeat(3, 1fr);
		gap: 2rem;
	}

	.priority-card {
		background: var(--off-white);
		padding: 2.5rem 2rem;
		border: 1px solid var(--border);
		text-align: center;
		position: relative;
		transition: transform 0.2s ease, box-shadow 0.2s ease;
	}

	.priority-card:hover {
		transform: translateY(-2px);
		box-shadow: 0 4px 20px rgba(0, 0, 0, 0.06);
	}

	.priority-card::before {
		content: '';
		position: absolute;
		top: -1px;
		left: 20%;
		right: 20%;
		height: 2px;
		background: linear-gradient(90deg, var(--navy), var(--red));
		opacity: 0.5;
	}

	.card-icon {
		font-size: 0.75rem;
		font-weight: 700;
		color: var(--red);
		margin-bottom: 1rem;
		letter-spacing: 0.2em;
	}

	.priority-card h3 {
		font-size: 1.375rem;
		color: var(--navy);
		margin-bottom: 1rem;
		font-weight: 700;
	}

	.priority-card p {
		font-size: 0.9375rem;
		line-height: 1.75;
		color: var(--muted);
	}

	/* ─── QUOTE BAND ─── */
	.quote-band {
		background: var(--red);
		padding: 4rem 0;
		text-align: center;
	}

	.quote-band blockquote {
		max-width: 600px;
		margin: 0 auto;
		font-size: clamp(1.125rem, 2.5vw, 1.375rem);
		font-style: italic;
		line-height: 1.8;
		color: var(--white);
		padding: 0 1rem;
	}

	.quote-band cite {
		display: block;
		margin-top: 1.25rem;
		font-size: 0.875rem;
		color: rgba(255, 255, 255, 0.65);
		font-style: normal;
		font-weight: 700;
		letter-spacing: 0.06em;
		text-transform: uppercase;
	}

	/* ─── SOUTIENS PREVIEW ─── */
	.soutiens-preview {
		padding: 5rem 0;
		background: var(--light);
	}

	.soutiens-logos {
		display: flex;
		justify-content: center;
		flex-wrap: wrap;
		gap: 1rem;
	}

	.soutien-badge {
		background: var(--white);
		border: 1px solid var(--border);
		padding: 0.75rem 1.5rem;
		font-size: 0.875rem;
		font-weight: 700;
		color: var(--navy);
		letter-spacing: 0.02em;
	}

	/* ─── CTA SECTION ─── */
	.cta-section {
		background: var(--navy-deep);
		color: var(--white);
		text-align: center;
		padding: 5rem 0;
	}

	.cta-section h2 {
		font-size: clamp(1.5rem, 3vw, 2.25rem);
		margin-bottom: 1rem;
		text-transform: uppercase;
		letter-spacing: 0.06em;
	}

	.cta-section p {
		color: rgba(255, 255, 255, 0.55);
		font-size: 1.0625rem;
		font-style: italic;
		margin-bottom: 2.5rem;
		line-height: 1.7;
	}

	.cta-buttons {
		display: flex;
		gap: 1rem;
		justify-content: center;
		flex-wrap: wrap;
	}

	/* ─── RESPONSIVE ─── */
	@media (max-width: 900px) {
		.priority-grid {
			grid-template-columns: 1fr;
			max-width: 480px;
			margin: 0 auto;
		}
	}

	@media (max-width: 768px) {
		.about-grid {
			grid-template-columns: 1fr;
			gap: 2rem;
			max-width: 500px;
			margin: 0 auto;
		}

		.about-portrait img {
			max-height: 360px;
			width: 100%;
		}

		.about-content .section-header {
			justify-content: center !important;
		}

		.about-content {
			text-align: center;
		}

		.facts-list {
			grid-template-columns: repeat(2, 1fr);
		}

		.facts-list li:nth-child(2) {
			border-right: none;
		}
	}

	@media (max-width: 640px) {
		.hero-inner {
			padding: 3rem 1.5rem 4rem;
		}

		.hero-photo {
			width: 120px;
			height: 120px;
		}

		.hero h1 {
			font-size: 1.75rem;
		}

		.geo {
			font-size: 0.6875rem;
			gap: 0.5rem;
		}

		.hero-intro {
			font-size: 1rem;
		}

		.hero-buttons,
		.cta-buttons {
			flex-direction: column;
			align-items: center;
		}

		.hero-buttons .btn,
		.cta-buttons .btn {
			width: 100%;
			max-width: 300px;
			text-align: center;
		}

		.priorities-overview,
		.about,
		.soutiens-preview {
			padding: 3.5rem 0;
		}

		.cta-section {
			padding: 3.5rem 0;
		}

		.facts-list {
			grid-template-columns: 1fr 1fr;
		}

		.soutiens-logos {
			flex-direction: column;
			align-items: center;
		}
	}
</style>
