---
import Head from '../../components/Head.astro';
import '../../styles/fonts.css';
---

<html lang="fr">
	<head>
		<Head
			title="Pascale Richard — Ensemble pour NY 2026"
			description="Candidate Renaissance pour les Français de New York, New Jersey et Connecticut. Journaliste, élue, engagée depuis 2016."
			path="/v/1"
		/>
	</head>
	<body>
		<!-- ═══ DARK HERO ═══ -->
		<section class="hero">
			<!-- SVG: Concentric arcs rising from bottom -->
			<svg class="svg-deco svg-skyline" viewBox="0 0 1200 260" fill="none" xmlns="http://www.w3.org/2000/svg" aria-hidden="true">
				<!-- Large concentric semicircles from bottom center -->
				<path d="M600 360 m-340 0 a340,340 0 0,1 680,0" stroke="currentColor" stroke-width="1.5" fill="none"/>
				<path d="M600 360 m-280 0 a280,280 0 0,1 560,0" stroke="currentColor" stroke-width="1.2" fill="none"/>
				<path d="M600 360 m-220 0 a220,220 0 0,1 440,0" stroke="currentColor" stroke-width="1" fill="none"/>
				<path d="M600 360 m-160 0 a160,160 0 0,1 320,0" stroke="currentColor" stroke-width="0.8" fill="none"/>
				<path d="M600 360 m-100 0 a100,100 0 0,1 200,0" stroke="currentColor" stroke-width="0.6" fill="none"/>
				<!-- Accent dots scattered along arcs -->
				<circle cx="180" cy="230" r="3" fill="currentColor" opacity="0.4"/>
				<circle cx="340" cy="185" r="2" fill="currentColor" opacity="0.35"/>
				<circle cx="500" cy="200" r="2.5" fill="currentColor" opacity="0.3"/>
				<circle cx="700" cy="195" r="2" fill="currentColor" opacity="0.35"/>
				<circle cx="860" cy="215" r="3" fill="currentColor" opacity="0.4"/>
				<circle cx="1020" cy="235" r="2.5" fill="currentColor" opacity="0.3"/>
				<circle cx="130" cy="248" r="2" fill="currentColor" opacity="0.25"/>
				<circle cx="1070" cy="250" r="2" fill="currentColor" opacity="0.25"/>
			</svg>

			<!-- SVG: Radiating starburst with concentric rings -->
			<svg class="svg-deco svg-bridge" viewBox="0 0 400 400" fill="none" xmlns="http://www.w3.org/2000/svg" aria-hidden="true">
				<!-- Radiating lines from center -->
				<line x1="200" y1="200" x2="200" y2="20" stroke="currentColor" stroke-width="0.7"/>
				<line x1="200" y1="200" x2="326" y2="42" stroke="currentColor" stroke-width="0.7"/>
				<line x1="200" y1="200" x2="380" y2="136" stroke="currentColor" stroke-width="0.7"/>
				<line x1="200" y1="200" x2="380" y2="264" stroke="currentColor" stroke-width="0.7"/>
				<line x1="200" y1="200" x2="326" y2="358" stroke="currentColor" stroke-width="0.7"/>
				<line x1="200" y1="200" x2="200" y2="380" stroke="currentColor" stroke-width="0.7"/>
				<line x1="200" y1="200" x2="74" y2="358" stroke="currentColor" stroke-width="0.7"/>
				<line x1="200" y1="200" x2="20" y2="264" stroke="currentColor" stroke-width="0.7"/>
				<line x1="200" y1="200" x2="20" y2="136" stroke="currentColor" stroke-width="0.7"/>
				<line x1="200" y1="200" x2="74" y2="42" stroke="currentColor" stroke-width="0.7"/>
				<!-- Concentric circles -->
				<circle cx="200" cy="200" r="55" stroke="currentColor" stroke-width="0.6" fill="none"/>
				<circle cx="200" cy="200" r="110" stroke="currentColor" stroke-width="0.5" fill="none"/>
				<circle cx="200" cy="200" r="165" stroke="currentColor" stroke-width="0.4" fill="none"/>
				<!-- Center dot -->
				<circle cx="200" cy="200" r="4" fill="currentColor" opacity="0.4"/>
			</svg>

			<nav>
				<img src="/assets/logos/RENAISSANCE_logo-complet_blanc.png" alt="Renaissance" class="nav-logo" />
			</nav>

			<div class="hero-inner">
				<div class="portrait-wrapper">
					<img src="/assets/images/pascale.jpg" alt="Pascale Richard" class="portrait" />
				</div>
				<h1>Pascale Richard</h1>
				<p class="role">Conseillère des Français de l'étranger</p>
				<div class="geo">
					<span>New York</span>
					<span class="dot"></span>
					<span>New Jersey</span>
					<span class="dot"></span>
					<span>Connecticut</span>
				</div>

				<div class="divider"></div>

				<p class="quote">« Le plus satisfaisant est d'aider mes concitoyens. Conseiller des Français de l'étranger est une fonction méconnue, mais les conseillers font un travail important. »</p>

				<ul class="facts">
					<li><strong>25+ ans</strong> à New York</li>
					<li><strong>Engagée</strong> depuis 2016</li>
					<li><strong>Élue</strong> en 2021</li>
					<li><strong>Suppléante</strong> de R. Lescure</li>
				</ul>

				<div class="buttons">
					<a href="https://www.instagram.com/pascalevrichard" target="_blank" rel="noopener noreferrer" class="cta">Suivez-nous sur Instagram</a>
					<a href="https://renaissance-fde.fr/portrait-dune-elue-a-votre-service-pascale-richard-new-york/" target="_blank" rel="noopener noreferrer" class="cta ghost">Découvrir Pascale</a>
				</div>
			</div>
			<div class="hero-stripe"></div>
		</section>

		<!-- ═══ PRIORITIES ═══ -->
		<section class="priorities">
			<!-- SVG: Nested hexagon grid — left -->
			<svg class="svg-deco svg-coffee" viewBox="0 0 200 240" fill="none" xmlns="http://www.w3.org/2000/svg" aria-hidden="true">
				<!-- Hexagon cluster -->
				<polygon points="100,20 145,42 145,88 100,110 55,88 55,42" stroke="currentColor" stroke-width="1.2" fill="none"/>
				<polygon points="100,40 130,55 130,85 100,100 70,85 70,55" stroke="currentColor" stroke-width="0.8" fill="none"/>
				<!-- Second hexagon offset below -->
				<polygon points="100,120 145,142 145,188 100,210 55,188 55,142" stroke="currentColor" stroke-width="1" fill="none"/>
				<polygon points="100,138 128,153 128,178 100,193 72,178 72,153" stroke="currentColor" stroke-width="0.6" fill="none"/>
				<!-- Connecting dots -->
				<circle cx="100" cy="110" r="2.5" fill="currentColor" opacity="0.4"/>
				<circle cx="100" cy="120" r="2.5" fill="currentColor" opacity="0.4"/>
				<!-- Corner accents -->
				<circle cx="55" cy="42" r="2" fill="currentColor" opacity="0.3"/>
				<circle cx="145" cy="42" r="2" fill="currentColor" opacity="0.3"/>
				<circle cx="55" cy="188" r="2" fill="currentColor" opacity="0.3"/>
				<circle cx="145" cy="188" r="2" fill="currentColor" opacity="0.3"/>
			</svg>

			<!-- SVG: Dot grid pattern — right -->
			<svg class="svg-deco svg-bag" viewBox="0 0 160 200" fill="none" xmlns="http://www.w3.org/2000/svg" aria-hidden="true">
				<!-- 6x8 dot grid -->
				<circle cx="20" cy="20" r="2.5" fill="currentColor" opacity="0.35"/>
				<circle cx="48" cy="20" r="2.5" fill="currentColor" opacity="0.3"/>
				<circle cx="76" cy="20" r="2.5" fill="currentColor" opacity="0.35"/>
				<circle cx="104" cy="20" r="2.5" fill="currentColor" opacity="0.3"/>
				<circle cx="132" cy="20" r="2.5" fill="currentColor" opacity="0.35"/>
				<circle cx="20" cy="48" r="2.5" fill="currentColor" opacity="0.3"/>
				<circle cx="48" cy="48" r="2.5" fill="currentColor" opacity="0.35"/>
				<circle cx="76" cy="48" r="2.5" fill="currentColor" opacity="0.3"/>
				<circle cx="104" cy="48" r="2.5" fill="currentColor" opacity="0.35"/>
				<circle cx="132" cy="48" r="2.5" fill="currentColor" opacity="0.3"/>
				<circle cx="20" cy="76" r="2.5" fill="currentColor" opacity="0.35"/>
				<circle cx="48" cy="76" r="2.5" fill="currentColor" opacity="0.3"/>
				<circle cx="76" cy="76" r="2.5" fill="currentColor" opacity="0.35"/>
				<circle cx="104" cy="76" r="2.5" fill="currentColor" opacity="0.3"/>
				<circle cx="132" cy="76" r="2.5" fill="currentColor" opacity="0.35"/>
				<circle cx="20" cy="104" r="2.5" fill="currentColor" opacity="0.3"/>
				<circle cx="48" cy="104" r="2.5" fill="currentColor" opacity="0.35"/>
				<circle cx="76" cy="104" r="2.5" fill="currentColor" opacity="0.3"/>
				<circle cx="104" cy="104" r="2.5" fill="currentColor" opacity="0.35"/>
				<circle cx="132" cy="104" r="2.5" fill="currentColor" opacity="0.3"/>
				<circle cx="20" cy="132" r="2.5" fill="currentColor" opacity="0.35"/>
				<circle cx="48" cy="132" r="2.5" fill="currentColor" opacity="0.3"/>
				<circle cx="76" cy="132" r="2.5" fill="currentColor" opacity="0.35"/>
				<circle cx="104" cy="132" r="2.5" fill="currentColor" opacity="0.3"/>
				<circle cx="132" cy="132" r="2.5" fill="currentColor" opacity="0.35"/>
				<circle cx="20" cy="160" r="2.5" fill="currentColor" opacity="0.3"/>
				<circle cx="48" cy="160" r="2.5" fill="currentColor" opacity="0.35"/>
				<circle cx="76" cy="160" r="2.5" fill="currentColor" opacity="0.3"/>
				<circle cx="104" cy="160" r="2.5" fill="currentColor" opacity="0.35"/>
				<circle cx="132" cy="160" r="2.5" fill="currentColor" opacity="0.3"/>
				<circle cx="20" cy="188" r="2.5" fill="currentColor" opacity="0.35"/>
				<circle cx="48" cy="188" r="2.5" fill="currentColor" opacity="0.3"/>
				<circle cx="76" cy="188" r="2.5" fill="currentColor" opacity="0.35"/>
				<circle cx="104" cy="188" r="2.5" fill="currentColor" opacity="0.3"/>
				<circle cx="132" cy="188" r="2.5" fill="currentColor" opacity="0.35"/>
			</svg>

			<!-- SVG: Wave scallop pattern — top -->
			<svg class="svg-deco svg-awning" viewBox="0 0 600 60" fill="none" xmlns="http://www.w3.org/2000/svg" aria-hidden="true">
				<path d="M0 30 Q25 8 50 30 Q75 52 100 30 Q125 8 150 30 Q175 52 200 30 Q225 8 250 30 Q275 52 300 30 Q325 8 350 30 Q375 52 400 30 Q425 8 450 30 Q475 52 500 30 Q525 8 550 30 Q575 52 600 30" stroke="currentColor" stroke-width="1.5" fill="none" stroke-linecap="round"/>
				<path d="M0 42 Q25 24 50 42 Q75 60 100 42 Q125 24 150 42 Q175 60 200 42 Q225 24 250 42 Q275 60 300 42 Q325 24 350 42 Q375 60 400 42 Q425 24 450 42 Q475 60 500 42 Q525 24 550 42 Q575 60 600 42" stroke="currentColor" stroke-width="0.8" fill="none" stroke-linecap="round"/>
			</svg>

			<div class="container">
				<p class="section-label">Engagements</p>
				<h2>Trois priorités pour notre communauté</h2>
				<div class="priority-grid">
					<div class="priority-card">
						<div class="card-accent"></div>
						<div class="card-number">01</div>
						<h3>Solidarité</h3>
						<p>Accompagner les Français en difficulté aux États-Unis — personnes âgées isolées, malades, familles touchées par la vie chère. Orienter chacun vers les bons interlocuteurs.</p>
					</div>
					<div class="priority-card">
						<div class="card-accent"></div>
						<div class="card-number">02</div>
						<h3>Lien & événements</h3>
						<p>Soutenir les associations et événements qui créent du lien — de Bastille Day aux salons de l'éducation bilingue — et rassembler les Français de toute la circonscription.</p>
					</div>
					<div class="priority-card">
						<div class="card-accent"></div>
						<div class="card-number">03</div>
						<h3>Services numériques</h3>
						<p>Accompagner la mise en place de France Consulaire et le vote par Internet. Tenir les Français informés, simplifier les démarches administratives à distance.</p>
					</div>
				</div>
			</div>
		</section>

		<!-- ═══ QUOTE BAND ═══ -->
		<section class="quote-band">
			<div class="container">
				<blockquote>
					« Il y a une résilience des gens qui n'attendent pas tout de leur gouvernement. C'est notre relation à l'État qui doit évoluer. »
				</blockquote>
			</div>
		</section>

		<!-- ═══ TIMELINE ═══ -->
		<section class="parcours">
			<!-- SVG: Concentric circles with arc segments — left -->
			<svg class="svg-deco svg-liberty" viewBox="0 0 200 400" fill="none" xmlns="http://www.w3.org/2000/svg" aria-hidden="true">
				<!-- Large concentric circles -->
				<circle cx="100" cy="200" r="160" stroke="currentColor" stroke-width="0.6" fill="none" stroke-dasharray="8 12"/>
				<circle cx="100" cy="200" r="120" stroke="currentColor" stroke-width="0.8" fill="none"/>
				<circle cx="100" cy="200" r="80" stroke="currentColor" stroke-width="1" fill="none"/>
				<circle cx="100" cy="200" r="40" stroke="currentColor" stroke-width="0.8" fill="none"/>
				<!-- Quarter-arc accents -->
				<path d="M100 80 A120 120 0 0 1 220 200" stroke="currentColor" stroke-width="1.5" fill="none" stroke-linecap="round"/>
				<path d="M100 320 A120 120 0 0 1 -20 200" stroke="currentColor" stroke-width="1.5" fill="none" stroke-linecap="round"/>
				<!-- Small dots at intersections -->
				<circle cx="100" cy="80" r="3" fill="currentColor" opacity="0.35"/>
				<circle cx="100" cy="320" r="3" fill="currentColor" opacity="0.35"/>
				<circle cx="220" cy="200" r="3" fill="currentColor" opacity="0.35"/>
				<circle cx="-20" cy="200" r="3" fill="currentColor" opacity="0.35"/>
				<circle cx="100" cy="200" r="4" fill="currentColor" opacity="0.3"/>
				<!-- Floating accent dots -->
				<circle cx="50" cy="100" r="2" fill="currentColor" opacity="0.25"/>
				<circle cx="160" cy="310" r="2" fill="currentColor" opacity="0.25"/>
				<circle cx="40" cy="280" r="1.5" fill="currentColor" opacity="0.2"/>
				<circle cx="170" cy="120" r="1.5" fill="currentColor" opacity="0.2"/>
			</svg>

			<!-- SVG: Interlocking triangles — right -->
			<svg class="svg-deco svg-subway" viewBox="0 0 160 160" fill="none" xmlns="http://www.w3.org/2000/svg" aria-hidden="true">
				<!-- Triangle pointing up -->
				<polygon points="80,10 148,128 12,128" stroke="currentColor" stroke-width="1.2" fill="none"/>
				<!-- Triangle pointing down -->
				<polygon points="80,150 148,32 12,32" stroke="currentColor" stroke-width="1.2" fill="none"/>
				<!-- Inner smaller triangles -->
				<polygon points="80,38 118,104 42,104" stroke="currentColor" stroke-width="0.7" fill="none"/>
				<polygon points="80,122 118,56 42,56" stroke="currentColor" stroke-width="0.7" fill="none"/>
				<!-- Center hexagon formed by overlap -->
				<polygon points="80,32 118,56 118,104 80,128 42,104 42,56" stroke="currentColor" stroke-width="0.5" fill="none" stroke-dasharray="4 4"/>
				<!-- Vertex dots -->
				<circle cx="80" cy="10" r="2.5" fill="currentColor" opacity="0.35"/>
				<circle cx="80" cy="150" r="2.5" fill="currentColor" opacity="0.35"/>
				<circle cx="12" cy="80" r="2" fill="currentColor" opacity="0.3"/>
				<circle cx="148" cy="80" r="2" fill="currentColor" opacity="0.3"/>
			</svg>

			<div class="container narrow">
				<p class="section-label">Parcours</p>
				<h2>D'une carrière dans les médias à l'engagement politique</h2>
				<div class="timeline">
					<div class="tl-item">
						<span class="tl-year">Années 2000</span>
						<div class="tl-content">
							<h4>Installation à New York</h4>
							<p>Carrière de journaliste. Rédactrice en chef de France Amérique. Directrice des événements culturels au Lycée Français de New York.</p>
						</div>
					</div>
					<div class="tl-item">
						<span class="tl-year">2016</span>
						<div class="tl-content">
							<h4>Engagement avec En Marche</h4>
							<p>S'engage dès la première heure avec le mouvement d'Emmanuel Macron. Campagne aux côtés de Roland Lescure pour l'Amérique du Nord.</p>
						</div>
					</div>
					<div class="tl-item">
						<span class="tl-year">2017</span>
						<div class="tl-content">
							<h4>Suppléante du député Lescure</h4>
							<p>Suppléante pendant la première mandature du député de la 1ère circonscription des Français établis hors de France.</p>
						</div>
					</div>
					<div class="tl-item">
						<span class="tl-year">2021</span>
						<div class="tl-content">
							<h4>Élue conseillère des Français de l'étranger</h4>
							<p>Tête de liste En Marche pour NY, NJ, CT et les Bermudes. Élue à l'Assemblée des Français de l'étranger, commission développement durable.</p>
						</div>
					</div>
					<div class="tl-item current">
						<span class="tl-year">2026</span>
						<div class="tl-content">
							<h4>Candidate aux législatives</h4>
							<p>Candidate Renaissance aux élections législatives pour représenter les Français de la circonscription tri-state.</p>
						</div>
					</div>
				</div>
			</div>
		</section>

		<!-- ═══ EVENTS ═══ -->
		<section class="events">
			<!-- SVG: Flowing wave curves — left -->
			<svg class="svg-deco svg-megaphone" viewBox="0 0 200 180" fill="none" xmlns="http://www.w3.org/2000/svg" aria-hidden="true">
				<!-- Parallel flowing waves -->
				<path d="M10 20 Q55 50 100 20 Q145 -10 190 20" stroke="currentColor" stroke-width="1.2" fill="none" stroke-linecap="round"/>
				<path d="M10 45 Q55 75 100 45 Q145 15 190 45" stroke="currentColor" stroke-width="1" fill="none" stroke-linecap="round"/>
				<path d="M10 70 Q55 100 100 70 Q145 40 190 70" stroke="currentColor" stroke-width="0.8" fill="none" stroke-linecap="round"/>
				<path d="M10 95 Q55 125 100 95 Q145 65 190 95" stroke="currentColor" stroke-width="1" fill="none" stroke-linecap="round"/>
				<path d="M10 120 Q55 150 100 120 Q145 90 190 120" stroke="currentColor" stroke-width="1.2" fill="none" stroke-linecap="round"/>
				<path d="M10 145 Q55 175 100 145 Q145 115 190 145" stroke="currentColor" stroke-width="0.8" fill="none" stroke-linecap="round"/>
				<!-- Accent dots between waves -->
				<circle cx="30" cy="55" r="2" fill="currentColor" opacity="0.3"/>
				<circle cx="100" cy="82" r="2.5" fill="currentColor" opacity="0.3"/>
				<circle cx="170" cy="108" r="2" fill="currentColor" opacity="0.3"/>
				<circle cx="60" cy="132" r="1.5" fill="currentColor" opacity="0.25"/>
				<circle cx="140" cy="32" r="1.5" fill="currentColor" opacity="0.25"/>
			</svg>

			<!-- SVG: Diamond lattice — right -->
			<svg class="svg-deco svg-calendar" viewBox="0 0 160 170" fill="none" xmlns="http://www.w3.org/2000/svg" aria-hidden="true">
				<!-- Diamond grid pattern -->
				<path d="M80 10 L140 50 L80 90 L20 50 Z" stroke="currentColor" stroke-width="1" fill="none"/>
				<path d="M80 50 L140 90 L80 130 L20 90 Z" stroke="currentColor" stroke-width="1" fill="none"/>
				<path d="M80 90 L140 130 L80 170 L20 130 Z" stroke="currentColor" stroke-width="1" fill="none"/>
				<!-- Inner diamonds -->
				<path d="M80 30 L110 50 L80 70 L50 50 Z" stroke="currentColor" stroke-width="0.6" fill="none"/>
				<path d="M80 70 L110 90 L80 110 L50 90 Z" stroke="currentColor" stroke-width="0.6" fill="none"/>
				<path d="M80 110 L110 130 L80 150 L50 130 Z" stroke="currentColor" stroke-width="0.6" fill="none"/>
				<!-- Junction dots -->
				<circle cx="80" cy="10" r="2" fill="currentColor" opacity="0.3"/>
				<circle cx="140" cy="50" r="2" fill="currentColor" opacity="0.3"/>
				<circle cx="80" cy="90" r="3" fill="currentColor" opacity="0.35"/>
				<circle cx="20" cy="50" r="2" fill="currentColor" opacity="0.3"/>
				<circle cx="140" cy="130" r="2" fill="currentColor" opacity="0.3"/>
				<circle cx="80" cy="170" r="2" fill="currentColor" opacity="0.3"/>
				<circle cx="20" cy="130" r="2" fill="currentColor" opacity="0.3"/>
			</svg>

			<div class="container">
				<p class="section-label">Événements</p>
				<h2>Rencontrons-nous</h2>
				<div class="events-placeholder">
					<p class="coming-soon">À venir</p>
					<p class="events-sub">Le calendrier des événements et rencontres sera bientôt disponible. Suivez-nous sur Instagram pour rester informé.</p>
				</div>
			</div>
		</section>

		<!-- ═══ CTA BOTTOM ═══ -->
		<section class="cta-bottom">
			<!-- SVG: Scattered arcs and dots — left -->
			<svg class="svg-deco svg-stars-left" viewBox="0 0 200 300" fill="none" xmlns="http://www.w3.org/2000/svg" aria-hidden="true">
				<!-- Arc segments at different angles -->
				<path d="M40 60 A50 50 0 0 1 90 40" stroke="currentColor" stroke-width="1.2" fill="none" stroke-linecap="round"/>
				<path d="M30 120 A40 40 0 0 0 70 100" stroke="currentColor" stroke-width="1" fill="none" stroke-linecap="round"/>
				<path d="M120 80 A35 35 0 0 1 155 95" stroke="currentColor" stroke-width="1" fill="none" stroke-linecap="round"/>
				<path d="M60 190 A60 60 0 0 1 120 170" stroke="currentColor" stroke-width="1.2" fill="none" stroke-linecap="round"/>
				<path d="M20 250 A45 45 0 0 0 65 240" stroke="currentColor" stroke-width="0.8" fill="none" stroke-linecap="round"/>
				<path d="M130 230 A30 30 0 0 1 160 245" stroke="currentColor" stroke-width="1" fill="none" stroke-linecap="round"/>
				<!-- Small circles -->
				<circle cx="60" cy="40" r="30" stroke="currentColor" stroke-width="0.6" fill="none"/>
				<circle cx="150" cy="160" r="25" stroke="currentColor" stroke-width="0.5" fill="none"/>
				<circle cx="40" cy="270" r="20" stroke="currentColor" stroke-width="0.5" fill="none"/>
				<!-- Dot accents -->
				<circle cx="90" cy="40" r="3" fill="currentColor" opacity="0.35"/>
				<circle cx="155" cy="95" r="2.5" fill="currentColor" opacity="0.3"/>
				<circle cx="120" cy="170" r="3" fill="currentColor" opacity="0.35"/>
				<circle cx="65" cy="240" r="2.5" fill="currentColor" opacity="0.3"/>
				<circle cx="160" cy="245" r="2" fill="currentColor" opacity="0.25"/>
				<circle cx="30" cy="150" r="2" fill="currentColor" opacity="0.25"/>
				<circle cx="100" cy="270" r="2" fill="currentColor" opacity="0.2"/>
			</svg>

			<!-- SVG: Grid dots with connecting lines — right -->
			<svg class="svg-deco svg-stars-right" viewBox="0 0 200 300" fill="none" xmlns="http://www.w3.org/2000/svg" aria-hidden="true">
				<!-- Diagonal crossing lines -->
				<line x1="20" y1="20" x2="180" y2="280" stroke="currentColor" stroke-width="0.5" stroke-dasharray="6 8"/>
				<line x1="180" y1="20" x2="20" y2="280" stroke="currentColor" stroke-width="0.5" stroke-dasharray="6 8"/>
				<line x1="100" y1="0" x2="100" y2="300" stroke="currentColor" stroke-width="0.5" stroke-dasharray="6 8"/>
				<!-- Circle at intersections -->
				<circle cx="100" cy="150" r="40" stroke="currentColor" stroke-width="0.8" fill="none"/>
				<circle cx="100" cy="150" r="20" stroke="currentColor" stroke-width="0.6" fill="none"/>
				<!-- Arc accents -->
				<path d="M40 60 A50 50 0 0 1 100 30" stroke="currentColor" stroke-width="1" fill="none" stroke-linecap="round"/>
				<path d="M100 270 A50 50 0 0 1 160 240" stroke="currentColor" stroke-width="1" fill="none" stroke-linecap="round"/>
				<!-- Dots at nodes -->
				<circle cx="100" cy="150" r="3" fill="currentColor" opacity="0.4"/>
				<circle cx="60" cy="60" r="2.5" fill="currentColor" opacity="0.3"/>
				<circle cx="140" cy="60" r="2.5" fill="currentColor" opacity="0.3"/>
				<circle cx="60" cy="240" r="2.5" fill="currentColor" opacity="0.3"/>
				<circle cx="140" cy="240" r="2.5" fill="currentColor" opacity="0.3"/>
				<circle cx="100" cy="30" r="2" fill="currentColor" opacity="0.25"/>
				<circle cx="100" cy="270" r="2" fill="currentColor" opacity="0.25"/>
				<circle cx="20" cy="150" r="2" fill="currentColor" opacity="0.2"/>
				<circle cx="180" cy="150" r="2" fill="currentColor" opacity="0.2"/>
			</svg>

			<div class="container">
				<h2>Rejoignez le mouvement</h2>
				<p class="cta-sub">Ensemble, construisons l'avenir de notre communauté française aux États-Unis.</p>
				<div class="buttons">
					<a href="https://www.instagram.com/pascalevrichard" target="_blank" rel="noopener noreferrer" class="cta">Suivez-nous sur Instagram</a>
					<a href="https://renaissance-fde.fr/portrait-dune-elue-a-votre-service-pascale-richard-new-york/" target="_blank" rel="noopener noreferrer" class="cta ghost-light">Découvrir Pascale</a>
				</div>
			</div>
		</section>

		<footer>
			<div class="footer-stripe"></div>
			<img src="/assets/logos/RENAISSANCE_monogramme_marine.png" alt="Renaissance" class="footer-logo" />
			<p>&copy; 2026 Pascale Richard — Ensemble pour NY 2026</p>
		</footer>
	</body>
</html>

<style>
	/* ═══════════════════════════════════════════════
	   BODEGA RETRO — NYC DELI / HAND-PAINTED AESTHETIC
	   Inspired by Zohran Mamdani campaign, Zabar's bags,
	   old-school bodega awnings, MetroCard colors.
	   ═══════════════════════════════════════════════ */

	:root {
		--deep-blue: #1a237e;
		--hot-orange: #FF6B35;
		--marigold: #FFD166;
		--cream: #FFF8E7;
		--off-white: #FFFDF5;
		--bodega-red: #D62828;
		--ink: #1B1B2F;
		--warm-gray: #5C5C6B;
		--card-bg: #FFF3D6;
		--border-tan: #E8D5A3;
		--shadow-color: rgba(26, 35, 126, 0.35);
	}

	* { margin: 0; padding: 0; box-sizing: border-box; }

	html {
		font-family: 'Maax', 'Arial Black', system-ui, sans-serif;
		color: var(--ink);
		scroll-behavior: smooth;
		font-size: 16px;
	}

	body {
		background: var(--cream);
		/* subtle paper grain texture */
		background-image:
			repeating-linear-gradient(
				0deg,
				transparent,
				transparent 2px,
				rgba(0, 0, 0, 0.008) 2px,
				rgba(0, 0, 0, 0.008) 4px
			),
			repeating-linear-gradient(
				90deg,
				transparent,
				transparent 2px,
				rgba(0, 0, 0, 0.005) 2px,
				rgba(0, 0, 0, 0.005) 4px
			);
	}

	.container {
		max-width: 1000px;
		margin: 0 auto;
		padding: 0 2rem;
	}

	.container.narrow {
		max-width: 720px;
	}

	/* ─── SVG DECORATIVE ILLUSTRATIONS ─── */
	.svg-deco {
		position: absolute;
		pointer-events: none;
		z-index: 0;
	}

	/* Hero: Concentric arcs across bottom */
	.svg-skyline {
		bottom: 20px;
		left: 0;
		width: 100%;
		height: auto;
		color: var(--marigold);
		opacity: 0.1;
	}

	/* Hero: Starburst (right side) */
	.svg-bridge {
		bottom: 60px;
		right: 2%;
		width: 320px;
		height: auto;
		color: var(--hot-orange);
		opacity: 0.08;
	}

	/* Priorities: Hexagon grid (left) */
	.svg-coffee {
		top: 60px;
		left: 3%;
		width: 180px;
		height: auto;
		color: var(--hot-orange);
		opacity: 0.08;
	}

	/* Priorities: Dot grid (right) */
	.svg-bag {
		bottom: 40px;
		right: 3%;
		width: 150px;
		height: auto;
		color: var(--deep-blue);
		opacity: 0.07;
	}

	/* Priorities: Wave scallop (top) */
	.svg-awning {
		top: 15px;
		left: 0;
		width: 100%;
		height: auto;
		color: var(--hot-orange);
		opacity: 0.08;
	}

	/* Parcours: Concentric circles (left) */
	.svg-liberty {
		top: 50%;
		left: 2%;
		transform: translateY(-50%);
		width: 200px;
		height: auto;
		color: var(--deep-blue);
		opacity: 0.07;
	}

	/* Parcours: Interlocking triangles (right) */
	.svg-subway {
		top: 80px;
		right: 3%;
		width: 160px;
		height: auto;
		color: var(--marigold);
		opacity: 0.08;
	}

	/* Events: Wave curves (left) */
	.svg-megaphone {
		top: 50%;
		left: 3%;
		transform: translateY(-50%);
		width: 180px;
		height: auto;
		color: var(--hot-orange);
		opacity: 0.08;
	}

	/* Events: Diamond lattice (right) */
	.svg-calendar {
		top: 50px;
		right: 3%;
		width: 150px;
		height: auto;
		color: var(--deep-blue);
		opacity: 0.07;
	}

	/* CTA: Arcs and dots left */
	.svg-stars-left {
		top: 0;
		left: 2%;
		width: 220px;
		height: auto;
		color: var(--marigold);
		opacity: 0.1;
	}

	/* CTA: Grid with lines right */
	.svg-stars-right {
		top: 0;
		right: 2%;
		width: 220px;
		height: auto;
		color: var(--hot-orange);
		opacity: 0.08;
	}

	/* ─── HERO (dark / bodega awning) ─── */
	.hero {
		background: var(--deep-blue);
		color: var(--cream);
		text-align: center;
		position: relative;
		overflow: hidden;
	}

	/* paper noise overlay on hero */
	.hero::before {
		content: '';
		position: absolute;
		inset: 0;
		background-image:
			radial-gradient(circle at 20% 80%, rgba(255, 107, 53, 0.1) 0%, transparent 50%),
			radial-gradient(circle at 80% 20%, rgba(255, 209, 102, 0.08) 0%, transparent 50%);
		pointer-events: none;
		z-index: 0;
	}

	.hero > * {
		position: relative;
		z-index: 1;
	}

	/* awning stripe at bottom of hero — single 5px repeating stripe */
	.hero-stripe {
		height: 6px;
		background: repeating-linear-gradient(
			90deg,
			var(--hot-orange) 0px,
			var(--hot-orange) 20px,
			var(--marigold) 20px,
			var(--marigold) 40px,
			var(--bodega-red) 40px,
			var(--bodega-red) 60px
		);
	}

	nav {
		padding: 2rem 2rem 0;
		text-align: center;
	}

	.nav-logo {
		height: 22px;
		opacity: 0.8;
		filter: drop-shadow(0 2px 4px rgba(0, 0, 0, 0.3));
	}

	.hero-inner {
		max-width: 660px;
		margin: 0 auto;
		padding: 3rem 2rem 5rem;
	}

	.portrait-wrapper {
		margin-bottom: 2rem;
	}

	.portrait {
		width: 164px;
		height: 164px;
		border-radius: 50%;
		object-fit: cover;
		object-position: center top;
		border: 5px solid var(--marigold);
		box-shadow:
			0 0 0 4px var(--hot-orange),
			6px 6px 0 0 rgba(0, 0, 0, 0.3);
	}

	.hero h1 {
		font-size: clamp(2.75rem, 7vw, 4.5rem);
		font-weight: 700;
		line-height: 1.0;
		margin-bottom: 0.5rem;
		color: var(--marigold);
		text-transform: uppercase;
		letter-spacing: 0.04em;
		text-shadow:
			4px 4px 0 var(--hot-orange),
			7px 7px 0 rgba(0, 0, 0, 0.4);
	}

	.role {
		font-size: 1.125rem;
		color: var(--cream);
		margin-bottom: 0.625rem;
		letter-spacing: 0.06em;
		font-weight: 700;
		text-shadow: 1px 1px 0 rgba(0, 0, 0, 0.3);
	}

	.geo {
		display: flex;
		justify-content: center;
		align-items: center;
		gap: 0.75rem;
		font-size: 0.875rem;
		color: var(--marigold);
		text-transform: uppercase;
		letter-spacing: 0.12em;
		font-weight: 700;
		margin-bottom: 2.5rem;
	}

	.dot {
		width: 8px;
		height: 8px;
		border-radius: 50%;
		background: var(--hot-orange);
		box-shadow: 0 0 8px rgba(255, 107, 53, 0.6);
	}

	.divider {
		width: 80px;
		height: 5px;
		margin: 0 auto 2rem;
		border-radius: 3px;
		background: repeating-linear-gradient(
			90deg,
			var(--hot-orange) 0px,
			var(--hot-orange) 12px,
			transparent 12px,
			transparent 18px,
			var(--marigold) 18px,
			var(--marigold) 30px,
			transparent 30px,
			transparent 36px
		);
	}

	.hero .quote {
		font-size: 1.15rem;
		font-style: italic;
		line-height: 1.75;
		color: rgba(255, 248, 231, 0.8);
		margin-bottom: 2rem;
		max-width: 520px;
		margin-left: auto;
		margin-right: auto;
		position: relative;
		padding: 0 1rem;
	}

	.facts {
		list-style: none;
		display: flex;
		flex-wrap: wrap;
		gap: 0.625rem;
		justify-content: center;
		margin-bottom: 3rem;
	}

	.facts li {
		font-size: 0.8125rem;
		color: var(--cream);
		background: rgba(255, 255, 255, 0.08);
		border: 2px solid rgba(255, 209, 102, 0.35);
		border-radius: 100px;
		padding: 0.4rem 1rem;
		font-weight: 700;
		text-transform: uppercase;
		letter-spacing: 0.04em;
	}

	.facts strong {
		color: var(--marigold);
	}

	.hero .buttons {
		display: flex;
		gap: 1rem;
		flex-wrap: wrap;
		justify-content: center;
	}

	/* ─── BUTTONS (bodega style) ─── */
	.cta {
		display: inline-block;
		padding: 1rem 2.25rem;
		border-radius: 8px;
		font-weight: 700;
		font-size: 1rem;
		text-decoration: none;
		text-transform: uppercase;
		letter-spacing: 0.06em;
		transition: all 0.15s ease;
		background: var(--hot-orange);
		color: var(--cream);
		border: 3px solid var(--hot-orange);
		box-shadow: 5px 5px 0 rgba(0, 0, 0, 0.35);
		cursor: pointer;
	}

	.cta:hover {
		background: #e85a24;
		transform: translate(-2px, -2px);
		box-shadow: 7px 7px 0 rgba(0, 0, 0, 0.4);
	}

	.cta:active {
		transform: translate(2px, 2px);
		box-shadow: 1px 1px 0 rgba(0, 0, 0, 0.3);
	}

	.cta.ghost {
		background: transparent;
		color: var(--marigold);
		border: 3px solid var(--marigold);
		box-shadow: 5px 5px 0 rgba(0, 0, 0, 0.3);
	}

	.cta.ghost:hover {
		background: rgba(255, 209, 102, 0.1);
		border-color: var(--cream);
		color: var(--cream);
		transform: translate(-2px, -2px);
		box-shadow: 7px 7px 0 rgba(0, 0, 0, 0.35);
	}

	.cta.ghost-light {
		background: transparent;
		color: var(--marigold);
		border: 3px solid rgba(255, 209, 102, 0.45);
		box-shadow: 5px 5px 0 rgba(0, 0, 0, 0.25);
	}

	.cta.ghost-light:hover {
		border-color: var(--cream);
		color: var(--cream);
		background: rgba(255, 209, 102, 0.08);
		transform: translate(-2px, -2px);
		box-shadow: 7px 7px 0 rgba(0, 0, 0, 0.3);
	}

	/* ─── PRIORITIES ─── */
	.priorities {
		padding: 6rem 0;
		background: var(--cream);
		position: relative;
		overflow: hidden;
	}

	/* decorative top awning stripe border */
	.priorities::before {
		content: '';
		position: absolute;
		top: 0;
		left: 0;
		right: 0;
		height: 6px;
		background: repeating-linear-gradient(
			90deg,
			var(--hot-orange) 0px,
			var(--hot-orange) 20px,
			var(--marigold) 20px,
			var(--marigold) 40px,
			var(--deep-blue) 40px,
			var(--deep-blue) 60px
		);
		z-index: 2;
	}

	.section-label {
		text-transform: uppercase;
		letter-spacing: 0.2em;
		font-size: 0.875rem;
		font-weight: 700;
		color: var(--hot-orange);
		margin-bottom: 0.625rem;
		text-align: center;
		text-shadow: 1px 1px 0 rgba(255, 107, 53, 0.15);
	}

	.priorities h2 {
		text-align: center;
		font-size: clamp(1.6rem, 3.5vw, 2.25rem);
		color: var(--deep-blue);
		margin-bottom: 3.5rem;
		line-height: 1.25;
		letter-spacing: 0.01em;
		text-shadow: 2px 2px 0 rgba(26, 35, 126, 0.1);
	}

	.priority-grid {
		display: grid;
		grid-template-columns: repeat(3, 1fr);
		gap: 1.75rem;
		position: relative;
		z-index: 1;
	}

	.priority-card {
		position: relative;
		background: var(--off-white);
		padding: 2.5rem 2rem;
		border-radius: 14px;
		overflow: hidden;
		border: 3px solid var(--border-tan);
		box-shadow: 6px 6px 0 rgba(26, 35, 126, 0.14);
		transition: transform 0.2s ease, box-shadow 0.2s ease;
	}

	.priority-card:hover {
		transform: translate(-3px, -3px);
		box-shadow: 9px 9px 0 rgba(26, 35, 126, 0.2);
	}

	.card-accent {
		position: absolute;
		top: 0;
		left: 0;
		width: 100%;
		height: 5px;
		background: repeating-linear-gradient(
			90deg,
			var(--hot-orange) 0px,
			var(--hot-orange) 8px,
			var(--marigold) 8px,
			var(--marigold) 16px
		);
	}

	.card-number {
		font-size: 0.875rem;
		font-weight: 700;
		color: var(--hot-orange);
		margin-bottom: 0.75rem;
		letter-spacing: 0.2em;
		text-transform: uppercase;
		line-height: 1;
	}

	.priority-card h3 {
		font-size: 1.3rem;
		color: var(--deep-blue);
		margin-bottom: 0.75rem;
		letter-spacing: 0.01em;
	}

	.priority-card p {
		font-size: 1rem;
		line-height: 1.7;
		color: var(--warm-gray);
	}

	/* ─── QUOTE BAND ─── */
	.quote-band {
		background: var(--hot-orange);
		padding: 5rem 0;
		position: relative;
		/* striped top/bottom borders */
		border-top: 5px solid var(--marigold);
		border-bottom: 5px solid var(--marigold);
	}

	.quote-band::before {
		content: '';
		position: absolute;
		inset: 0;
		background-image:
			repeating-linear-gradient(
				-45deg,
				transparent 0px,
				transparent 20px,
				rgba(255, 255, 255, 0.04) 20px,
				rgba(255, 255, 255, 0.04) 40px
			);
		pointer-events: none;
	}

	.quote-band blockquote {
		max-width: 640px;
		margin: 0 auto;
		font-size: clamp(1.2rem, 2.5vw, 1.5rem);
		font-style: italic;
		line-height: 1.7;
		color: var(--cream);
		text-align: center;
		position: relative;
		padding: 0 2rem;
		text-shadow: 1px 1px 0 rgba(0, 0, 0, 0.15);
		font-weight: 700;
	}

	.quote-band blockquote::before {
		content: '';
		display: block;
		width: 60px;
		height: 5px;
		margin: 0 auto 1.5rem;
		border-radius: 3px;
		background: repeating-linear-gradient(
			90deg,
			var(--cream) 0px,
			var(--cream) 10px,
			transparent 10px,
			transparent 16px
		);
	}

	/* ─── TIMELINE ─── */
	.parcours {
		padding: 6rem 0;
		background: var(--off-white);
		position: relative;
		overflow: hidden;
	}

	.parcours::before {
		content: '';
		position: absolute;
		top: 0;
		left: 0;
		right: 0;
		height: 6px;
		background: repeating-linear-gradient(
			90deg,
			var(--deep-blue) 0px,
			var(--deep-blue) 20px,
			var(--marigold) 20px,
			var(--marigold) 40px,
			var(--hot-orange) 40px,
			var(--hot-orange) 60px
		);
		z-index: 2;
	}

	.parcours .section-label {
		margin-bottom: 0.625rem;
	}

	.parcours h2 {
		text-align: center;
		font-size: clamp(1.5rem, 3vw, 2rem);
		color: var(--deep-blue);
		margin-bottom: 3.5rem;
		line-height: 1.3;
		letter-spacing: 0.01em;
		text-shadow: 2px 2px 0 rgba(26, 35, 126, 0.08);
	}

	.timeline {
		position: relative;
		padding-left: 4rem;
		z-index: 1;
	}

	.timeline::before {
		content: '';
		position: absolute;
		left: 0.75rem;
		top: 0.375rem;
		bottom: 0.375rem;
		width: 4px;
		background: repeating-linear-gradient(
			180deg,
			var(--marigold) 0px,
			var(--marigold) 8px,
			transparent 8px,
			transparent 14px
		);
		border-radius: 2px;
	}

	.tl-item {
		position: relative;
		padding-bottom: 2.75rem;
	}

	.tl-item:last-child {
		padding-bottom: 0;
	}

	.tl-item::before {
		content: '';
		position: absolute;
		left: -4rem;
		top: 0.375rem;
		width: 16px;
		height: 16px;
		border-radius: 50%;
		background: var(--cream);
		border: 4px solid var(--marigold);
		transform: translateX(calc(0.75rem - 50% + 2px));
		z-index: 1;
		box-shadow: 2px 2px 0 rgba(0, 0, 0, 0.15);
	}

	.tl-item.current::before {
		background: var(--hot-orange);
		border-color: var(--hot-orange);
		box-shadow:
			0 0 0 5px rgba(255, 107, 53, 0.2),
			3px 3px 0 rgba(0, 0, 0, 0.15);
	}

	.tl-year {
		display: inline-block;
		font-size: 0.8125rem;
		font-weight: 700;
		color: var(--cream);
		background: var(--deep-blue);
		padding: 0.2rem 0.75rem;
		border-radius: 4px;
		text-transform: uppercase;
		letter-spacing: 0.08em;
		margin-bottom: 0.5rem;
		box-shadow: 3px 3px 0 rgba(0, 0, 0, 0.18);
	}

	.tl-item.current .tl-year {
		background: var(--hot-orange);
		color: var(--cream);
	}

	.tl-content h4 {
		font-size: 1.125rem;
		color: var(--deep-blue);
		margin-bottom: 0.375rem;
		font-weight: 700;
		letter-spacing: 0.01em;
	}

	.tl-content p {
		font-size: 1rem;
		line-height: 1.7;
		color: var(--warm-gray);
	}

	/* ─── EVENTS ─── */
	.events {
		padding: 6rem 0;
		background: var(--cream);
		position: relative;
		overflow: hidden;
	}

	/* awning stripe top border on events */
	.events::before {
		content: '';
		position: absolute;
		top: 0;
		left: 0;
		right: 0;
		height: 6px;
		background: repeating-linear-gradient(
			90deg,
			var(--hot-orange) 0px,
			var(--hot-orange) 20px,
			var(--marigold) 20px,
			var(--marigold) 40px,
			var(--deep-blue) 40px,
			var(--deep-blue) 60px
		);
		z-index: 2;
	}

	.events h2 {
		text-align: center;
		font-size: clamp(1.5rem, 3vw, 2rem);
		color: var(--deep-blue);
		margin-bottom: 3rem;
		line-height: 1.3;
		letter-spacing: 0.01em;
		text-shadow: 2px 2px 0 rgba(26, 35, 126, 0.08);
	}

	.events-placeholder {
		text-align: center;
		border: 4px dashed var(--border-tan);
		border-radius: 16px;
		padding: 4rem 2rem;
		max-width: 560px;
		margin: 0 auto;
		background: var(--off-white);
		box-shadow: 5px 5px 0 rgba(26, 35, 126, 0.1);
		position: relative;
		z-index: 1;
	}

	.coming-soon {
		font-size: 1.25rem;
		font-weight: 700;
		color: var(--hot-orange);
		text-transform: uppercase;
		letter-spacing: 0.15em;
		margin-bottom: 0.75rem;
		text-shadow: 1px 1px 0 rgba(255, 107, 53, 0.15);
	}

	.events-sub {
		font-size: 1rem;
		line-height: 1.7;
		color: var(--warm-gray);
		max-width: 400px;
		margin: 0 auto;
	}

	/* ─── CTA BOTTOM ─── */
	.cta-bottom {
		background: var(--deep-blue);
		padding: 6rem 0;
		text-align: center;
		color: var(--cream);
		position: relative;
		overflow: hidden;
		border-top: 6px solid var(--marigold);
	}

	.cta-bottom::before {
		content: '';
		position: absolute;
		inset: 0;
		background-image:
			radial-gradient(circle at 10% 90%, rgba(255, 107, 53, 0.1) 0%, transparent 40%),
			radial-gradient(circle at 90% 10%, rgba(255, 209, 102, 0.08) 0%, transparent 40%),
			repeating-linear-gradient(
				45deg,
				transparent 0px,
				transparent 30px,
				rgba(255, 255, 255, 0.018) 30px,
				rgba(255, 255, 255, 0.018) 60px
			);
		pointer-events: none;
	}

	.cta-bottom > .container {
		position: relative;
		z-index: 1;
	}

	.cta-bottom h2 {
		font-size: clamp(1.75rem, 4vw, 2.75rem);
		margin-bottom: 1rem;
		color: var(--marigold);
		text-transform: uppercase;
		letter-spacing: 0.03em;
		text-shadow: 4px 4px 0 var(--hot-orange);
	}

	.cta-sub {
		font-size: 1.125rem;
		color: rgba(255, 248, 231, 0.65);
		margin-bottom: 2.5rem;
		line-height: 1.6;
	}

	.cta-bottom .buttons {
		display: flex;
		gap: 1rem;
		justify-content: center;
		flex-wrap: wrap;
	}

	/* ─── FOOTER ─── */
	footer {
		text-align: center;
		padding: 2.5rem 2rem;
		font-size: 0.875rem;
		color: var(--warm-gray);
		border-top: 4px solid var(--border-tan);
		display: flex;
		flex-direction: column;
		align-items: center;
		gap: 1rem;
		background: var(--cream);
		position: relative;
	}

	.footer-stripe {
		width: 100%;
		height: 5px;
		background: repeating-linear-gradient(
			90deg,
			var(--hot-orange) 0px,
			var(--hot-orange) 20px,
			var(--marigold) 20px,
			var(--marigold) 40px,
			var(--deep-blue) 40px,
			var(--deep-blue) 60px
		);
	}

	.footer-logo {
		height: 36px;
		width: auto;
		opacity: 0.5;
	}

	/* ─── RESPONSIVE ─── */
	@media (max-width: 900px) {
		.priority-grid {
			grid-template-columns: 1fr;
			max-width: 480px;
			margin: 0 auto;
		}

		/* Scale down side SVGs on tablets */
		.svg-coffee { width: 140px; opacity: 0.06; }
		.svg-bag { width: 110px; opacity: 0.05; }
		.svg-liberty { width: 150px; opacity: 0.05; }
		.svg-subway { width: 120px; opacity: 0.06; }
		.svg-megaphone { width: 140px; opacity: 0.06; }
		.svg-calendar { width: 110px; opacity: 0.05; }
		.svg-stars-left { width: 170px; }
		.svg-stars-right { width: 170px; }
	}

	@media (max-width: 640px) {
		.hero-inner {
			padding: 2.5rem 1.5rem 4rem;
		}

		.portrait {
			width: 132px;
			height: 132px;
			border-width: 4px;
		}

		.hero h1 {
			font-size: 2.5rem;
			text-shadow:
				3px 3px 0 var(--hot-orange),
				4px 4px 0 rgba(0, 0, 0, 0.3);
		}

		.role {
			font-size: 0.9375rem;
		}

		.geo {
			font-size: 0.75rem;
			gap: 0.5rem;
		}

		.hero .quote {
			font-size: 1rem;
		}

		.facts {
			flex-direction: column;
			align-items: center;
			gap: 0.5rem;
		}

		.facts li {
			font-size: 0.75rem;
		}

		.cta {
			font-size: 0.9375rem;
			padding: 0.875rem 1.75rem;
			width: 100%;
			text-align: center;
			box-shadow: 3px 3px 0 rgba(0, 0, 0, 0.25);
		}

		.cta:hover {
			transform: translate(-1px, -1px);
			box-shadow: 4px 4px 0 rgba(0, 0, 0, 0.3);
		}

		.hero .buttons,
		.cta-bottom .buttons {
			flex-direction: column;
			align-items: center;
		}

		.priorities,
		.parcours,
		.events {
			padding: 4rem 0;
		}

		.priorities h2,
		.parcours h2,
		.events h2 {
			margin-bottom: 2.5rem;
			font-size: 1.5rem;
		}

		.quote-band {
			padding: 3.5rem 0;
		}

		.cta-bottom {
			padding: 4rem 0;
		}

		.cta-bottom h2 {
			text-shadow: 2px 2px 0 var(--hot-orange);
		}

		.priority-card {
			padding: 2rem 1.5rem;
			border-radius: 12px;
		}

		.card-number {
			font-size: 0.8125rem;
		}

		.timeline {
			padding-left: 3rem;
		}

		.tl-item::before {
			left: -3rem;
		}

		.tl-item {
			padding-bottom: 2.25rem;
		}

		.events-placeholder {
			padding: 3rem 1.5rem;
		}

		/* Hide side decorative SVGs on small screens, keep background ones */
		.svg-coffee,
		.svg-bag,
		.svg-liberty,
		.svg-subway,
		.svg-megaphone,
		.svg-calendar {
			display: none;
		}

		/* Keep but reduce arcs */
		.svg-skyline {
			opacity: 0.06;
		}

		.svg-bridge {
			display: none;
		}

		/* Reduce CTA decorations on mobile */
		.svg-stars-left { width: 140px; opacity: 0.07; }
		.svg-stars-right { width: 140px; opacity: 0.06; }
	}
</style>
