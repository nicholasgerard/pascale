---
import Head from '../../components/Head.astro';
import '../../styles/fonts.css';
---

<html lang="fr">
	<head>
		<Head
			title="Pascale Richard — Ensemble pour NY 2026"
			description="Candidate Renaissance pour les Français de New York, New Jersey et Connecticut. Journaliste, élue, engagée depuis 2016."
			path="/v/3"
		/>
	</head>
	<body>
		<!-- ═══ DARK HERO (not sticky) ═══ -->
		<section class="hero">
			<nav>
				<img src="/assets/logos/RENAISSANCE_logo-complet_blanc.png" alt="Renaissance" class="nav-logo" />
			</nav>

			<div class="hero-inner">
				<div class="portrait-wrapper">
					<img src="/assets/images/pascale.jpg" alt="Pascale Richard" class="portrait" />
				</div>
				<h1>Pascale Richard</h1>
				<p class="role">Conseillère des Français de l'étranger</p>
				<div class="geo">
					<span>New York</span>
					<span class="dot"></span>
					<span>New Jersey</span>
					<span class="dot"></span>
					<span>Connecticut</span>
				</div>

				<div class="divider"></div>

				<p class="quote">&laquo; Le plus satisfaisant est d'aider mes concitoyens. Conseiller des Français de l'étranger est une fonction méconnue, mais les conseillers font un travail important. &raquo;</p>

				<ul class="facts">
					<li><strong>25+ ans</strong> à New York</li>
					<li><strong>Engagée</strong> depuis 2016</li>
					<li><strong>Élue</strong> en 2021</li>
					<li><strong>Suppléante</strong> de R. Lescure</li>
				</ul>

				<div class="buttons">
					<a href="https://www.instagram.com/pascalevrichard" target="_blank" rel="noopener noreferrer" class="cta">Suivez-nous sur Instagram</a>
					<a href="https://renaissance-fde.fr/portrait-dune-elue-a-votre-service-pascale-richard-new-york/" target="_blank" rel="noopener noreferrer" class="cta ghost">Découvrir Pascale</a>
				</div>
			</div>
		</section>

		<!-- ═══ PRIORITIES (sticky header — top: 0) ═══ -->
		<section class="section-block priorities-section">
			<div class="sticky-header sticky-1">
				<div class="sticky-header-inner">
					<span class="section-num">01</span>
					<div class="sticky-header-text">
						<span class="section-label-sticky">Engagements</span>
						<span class="section-title-sticky">Trois priorités pour notre communauté</span>
					</div>
				</div>
				<div class="accent-bar"></div>
			</div>
			<div class="section-content priorities-content">
				<div class="priority-card">
					<div class="card-left-accent"></div>
					<div class="card-inner">
						<span class="card-number">01</span>
						<h3>Solidarité</h3>
						<p>Accompagner les Français en difficulté aux États-Unis — personnes âgées isolées, malades, familles touchées par la vie chère. Orienter chacun vers les bons interlocuteurs.</p>
					</div>
				</div>
				<div class="priority-card">
					<div class="card-left-accent"></div>
					<div class="card-inner">
						<span class="card-number">02</span>
						<h3>Lien &amp; événements</h3>
						<p>Soutenir les associations et événements qui créent du lien — de Bastille Day aux salons de l'éducation bilingue — et rassembler les Français de toute la circonscription.</p>
					</div>
				</div>
				<div class="priority-card">
					<div class="card-left-accent"></div>
					<div class="card-inner">
						<span class="card-number">03</span>
						<h3>Services numériques</h3>
						<p>Accompagner la mise en place de France Consulaire et le vote par Internet. Tenir les Français informés, simplifier les démarches administratives à distance.</p>
					</div>
				</div>
			</div>
		</section>

		<!-- ═══ QUOTE BAND ═══ -->
		<section class="quote-band">
			<div class="quote-band-inner">
				<blockquote>
					Il y a une résilience des gens qui n'attendent pas tout de leur gouvernement. C'est notre relation à l'État qui doit évoluer.
				</blockquote>
			</div>
		</section>

		<!-- ═══ TIMELINE (sticky header — top: 70px) ═══ -->
		<section class="section-block parcours-section">
			<div class="sticky-header sticky-2">
				<div class="sticky-header-inner">
					<span class="section-num">02</span>
					<div class="sticky-header-text">
						<span class="section-label-sticky">Parcours</span>
						<span class="section-title-sticky">D'une carrière dans les médias à l'engagement politique</span>
					</div>
				</div>
				<div class="accent-bar"></div>
			</div>
			<div class="section-content timeline-content">
				<div class="timeline">
					<div class="tl-item">
						<div class="tl-marker"></div>
						<span class="tl-year">Années 2000</span>
						<h4>Installation à New York</h4>
						<p>Carrière de journaliste. Rédactrice en chef de France Amérique. Directrice des événements culturels au Lycée Français de New York.</p>
					</div>
					<div class="tl-item">
						<div class="tl-marker"></div>
						<span class="tl-year">2016</span>
						<h4>Engagement avec En Marche</h4>
						<p>S'engage dès la première heure avec le mouvement d'Emmanuel Macron. Campagne aux côtés de Roland Lescure pour l'Amérique du Nord.</p>
					</div>
					<div class="tl-item">
						<div class="tl-marker"></div>
						<span class="tl-year">2017</span>
						<h4>Suppléante du député Lescure</h4>
						<p>Suppléante pendant la première mandature du député de la 1ère circonscription des Français établis hors de France.</p>
					</div>
					<div class="tl-item">
						<div class="tl-marker"></div>
						<span class="tl-year">2021</span>
						<h4>Élue conseillère des Français de l'étranger</h4>
						<p>Tête de liste En Marche pour NY, NJ, CT et les Bermudes. Élue à l'Assemblée des Français de l'étranger, commission développement durable.</p>
					</div>
					<div class="tl-item current">
						<div class="tl-marker"></div>
						<span class="tl-year">2026</span>
						<h4>Candidate aux législatives</h4>
						<p>Candidate Renaissance aux élections législatives pour représenter les Français de la circonscription tri-state.</p>
					</div>
				</div>
			</div>
		</section>

		<!-- ═══ EVENTS (sticky header — top: 140px) ═══ -->
		<section class="section-block events-section">
			<div class="sticky-header sticky-3">
				<div class="sticky-header-inner">
					<span class="section-num">03</span>
					<div class="sticky-header-text">
						<span class="section-label-sticky">Événements</span>
						<span class="section-title-sticky">Rencontrons-nous</span>
					</div>
				</div>
				<div class="accent-bar"></div>
			</div>
			<div class="section-content events-content">
				<div class="events-placeholder">
					<p class="coming-soon">À venir</p>
					<p class="events-sub">Le calendrier des événements et rencontres sera bientôt disponible. Suivez-nous sur Instagram pour rester informé.</p>
				</div>
			</div>
		</section>

		<!-- ═══ CTA BOTTOM ═══ -->
		<section class="cta-bottom">
			<div class="cta-top-accent"></div>
			<div class="cta-bottom-inner">
				<h2>Rejoignez le mouvement</h2>
				<p class="cta-sub">Ensemble, construisons l'avenir de notre communauté française aux États-Unis.</p>
				<div class="buttons">
					<a href="https://www.instagram.com/pascalevrichard" target="_blank" rel="noopener noreferrer" class="cta">Suivez-nous sur Instagram</a>
					<a href="https://renaissance-fde.fr/portrait-dune-elue-a-votre-service-pascale-richard-new-york/" target="_blank" rel="noopener noreferrer" class="cta ghost-light">Découvrir Pascale</a>
				</div>
			</div>
		</section>

		<!-- ═══ FOOTER ═══ -->
		<footer>
			<img src="/assets/logos/RENAISSANCE_monogramme_marine.png" alt="Renaissance" class="footer-logo" />
			<p>&copy; 2026 Pascale Richard &mdash; Ensemble pour NY 2026</p>
		</footer>
	</body>
</html>

<style>
	/* ─── VARIABLES ─── */
	:root {
		--navy: #001B6E;
		--blue: #002395;
		--red: #ED2939;
		--white: #FFFFFF;
		--warm-white: #FAFAFA;
		--gold: #C9A84C;
		--text: #1A1A2E;
		--light: #F0F0F5;
		--border: #E2E2EA;
		--sticky-h: 70px;
	}

	/* ─── RESET ─── */
	* { margin: 0; padding: 0; box-sizing: border-box; }

	html {
		font-family: 'Maax', system-ui, -apple-system, sans-serif;
		color: var(--text);
		scroll-behavior: smooth;
		font-size: 16px;
		background: var(--warm-white);
	}

	body {
		line-height: 1.7;
		-webkit-font-smoothing: antialiased;
		-moz-osx-font-smoothing: grayscale;
	}

	/* ═══════════════════════════════════════
	   HERO — full intro, NOT sticky
	   ═══════════════════════════════════════ */
	.hero {
		background: var(--navy);
		color: var(--white);
		text-align: center;
		position: relative;
		z-index: 5;
	}

	nav {
		padding: 2rem 2rem 0;
		text-align: center;
	}

	.nav-logo {
		height: 22px;
		opacity: 0.7;
	}

	.hero-inner {
		max-width: 640px;
		margin: 0 auto;
		padding: 3rem 2rem 5rem;
	}

	.portrait-wrapper {
		margin-bottom: 2rem;
	}

	.portrait {
		width: 152px;
		height: 152px;
		border-radius: 50%;
		object-fit: cover;
		object-position: center top;
		border: 3px solid var(--gold);
		box-shadow: 0 0 50px rgba(201, 168, 76, 0.18);
	}

	.hero h1 {
		font-size: clamp(2.5rem, 6vw, 4rem);
		font-weight: 700;
		line-height: 1.08;
		margin-bottom: 0.625rem;
		color: var(--white);
		letter-spacing: -0.02em;
	}

	.role {
		font-size: 1.125rem;
		color: rgba(255, 255, 255, 0.7);
		margin-bottom: 0.625rem;
		line-height: 1.5;
	}

	.geo {
		display: flex;
		justify-content: center;
		align-items: center;
		gap: 0.75rem;
		font-size: 0.875rem;
		color: rgba(255, 255, 255, 0.45);
		text-transform: uppercase;
		letter-spacing: 0.1em;
		margin-bottom: 2.5rem;
		line-height: 1;
	}

	.dot {
		width: 4px;
		height: 4px;
		border-radius: 50%;
		background: var(--gold);
		flex-shrink: 0;
	}

	.divider {
		width: 50px;
		height: 3px;
		background: var(--gold);
		margin: 0 auto 2rem;
		border-radius: 2px;
	}

	.hero .quote {
		font-size: 1.125rem;
		font-style: italic;
		line-height: 1.8;
		color: rgba(255, 255, 255, 0.75);
		margin-bottom: 2rem;
		max-width: 500px;
		margin-left: auto;
		margin-right: auto;
	}

	.facts {
		list-style: none;
		display: flex;
		flex-wrap: wrap;
		gap: 0.5rem 1.5rem;
		justify-content: center;
		margin-bottom: 3rem;
	}

	.facts li {
		font-size: 0.875rem;
		color: rgba(255, 255, 255, 0.5);
		line-height: 1.5;
	}

	.facts strong {
		color: var(--gold);
	}

	.hero .buttons {
		display: flex;
		gap: 1rem;
		flex-wrap: wrap;
		justify-content: center;
	}

	/* ─── SHARED BUTTONS ─── */
	.cta {
		display: inline-block;
		padding: 0.875rem 2.25rem;
		border-radius: 4px;
		font-weight: 700;
		font-size: 1rem;
		text-decoration: none;
		transition: all 0.2s ease;
		background: var(--red);
		color: var(--white);
		line-height: 1.2;
	}

	.cta:hover {
		background: #c4212f;
	}

	.cta.ghost {
		background: transparent;
		color: var(--white);
		border: 2px solid rgba(255, 255, 255, 0.3);
	}

	.cta.ghost:hover {
		border-color: var(--white);
		background: rgba(255, 255, 255, 0.08);
	}

	.cta.ghost-light {
		background: transparent;
		color: var(--white);
		border: 2px solid rgba(255, 255, 255, 0.35);
	}

	.cta.ghost-light:hover {
		border-color: var(--white);
		background: rgba(255, 255, 255, 0.1);
	}

	/* ═══════════════════════════════════════
	   STICKY SECTION HEADERS
	   Core design pattern: each section has
	   a pinned header that stacks visually.
	   Headers are ~70px tall; each subsequent
	   header offsets by 70px so they pile up.
	   ═══════════════════════════════════════ */
	.section-block {
		position: relative;
	}

	.sticky-header {
		position: sticky;
		z-index: 10;
		background: rgba(250, 250, 250, 0.92);
		backdrop-filter: blur(12px);
		-webkit-backdrop-filter: blur(12px);
		border-bottom: 1px solid var(--border);
	}

	/* Stacking offsets */
	.sticky-1 { top: 0; }
	.sticky-2 { top: var(--sticky-h); }
	.sticky-3 { top: calc(var(--sticky-h) * 2); }

	.sticky-header-inner {
		max-width: 900px;
		margin: 0 auto;
		padding: 1.125rem 2rem;
		display: flex;
		align-items: center;
		gap: 1.25rem;
	}

	.section-num {
		font-size: 2.5rem;
		font-weight: 700;
		color: var(--blue);
		opacity: 0.18;
		line-height: 1;
		letter-spacing: -0.02em;
		flex-shrink: 0;
	}

	.sticky-header-text {
		display: flex;
		flex-direction: column;
		gap: 0.125rem;
	}

	.section-label-sticky {
		text-transform: uppercase;
		letter-spacing: 0.15em;
		font-size: 0.6875rem;
		font-weight: 700;
		color: var(--red);
		line-height: 1.2;
	}

	.section-title-sticky {
		font-size: 1.125rem;
		font-weight: 700;
		color: var(--blue);
		line-height: 1.3;
		letter-spacing: -0.01em;
	}

	.accent-bar {
		height: 3px;
		background: linear-gradient(90deg, var(--blue), var(--red));
	}

	/* ═══════════════════════════════════════
	   PRIORITIES — editorial single-column
	   ═══════════════════════════════════════ */
	.priorities-section {
		background: var(--warm-white);
	}

	.section-content {
		max-width: 640px;
		margin: 0 auto;
		padding: 3.5rem 2rem 5rem;
	}

	.priorities-content {
		display: flex;
		flex-direction: column;
		gap: 1.75rem;
	}

	.priority-card {
		display: flex;
		background: var(--white);
		border-radius: 8px;
		overflow: hidden;
		border: 1px solid var(--border);
		transition: box-shadow 0.25s ease;
	}

	.priority-card:hover {
		box-shadow: 0 4px 24px rgba(0, 35, 149, 0.06);
	}

	.card-left-accent {
		width: 5px;
		flex-shrink: 0;
		background: linear-gradient(180deg, var(--blue), var(--red));
	}

	.card-inner {
		padding: 2rem 2.25rem;
	}

	.card-number {
		display: inline-block;
		font-size: 0.75rem;
		font-weight: 700;
		color: var(--red);
		letter-spacing: 0.08em;
		margin-bottom: 0.75rem;
		text-transform: uppercase;
	}

	.priority-card h3 {
		font-size: 1.25rem;
		font-weight: 700;
		color: var(--blue);
		margin-bottom: 0.625rem;
		line-height: 1.3;
		letter-spacing: -0.01em;
	}

	.priority-card p {
		font-size: 1rem;
		line-height: 1.85;
		color: #555;
	}

	/* ═══════════════════════════════════════
	   QUOTE BAND — marks as pseudo-elements
	   ═══════════════════════════════════════ */
	.quote-band {
		background: var(--blue);
		padding: 5rem 2rem;
		position: relative;
		z-index: 5;
	}

	.quote-band-inner {
		max-width: 640px;
		margin: 0 auto;
		text-align: center;
		position: relative;
	}

	.quote-band blockquote {
		font-size: clamp(1.125rem, 2.5vw, 1.375rem);
		font-style: italic;
		line-height: 1.85;
		color: var(--white);
		position: relative;
		padding: 0 2.5rem;
	}

	.quote-band blockquote::before {
		content: '\00AB';
		position: absolute;
		left: -0.25rem;
		top: -0.15em;
		font-size: 3rem;
		color: var(--gold);
		opacity: 0.5;
		font-style: italic;
		line-height: 1;
	}

	.quote-band blockquote::after {
		content: '\00BB';
		position: absolute;
		right: -0.25rem;
		bottom: -0.15em;
		font-size: 3rem;
		color: var(--gold);
		opacity: 0.5;
		font-style: italic;
		line-height: 1;
	}

	/* ═══════════════════════════════════════
	   TIMELINE
	   ═══════════════════════════════════════ */
	.parcours-section {
		background: var(--light);
	}

	.timeline {
		position: relative;
		padding-left: 3.5rem;
	}

	.timeline::before {
		content: '';
		position: absolute;
		left: 6px;
		top: 6px;
		bottom: 6px;
		width: 2px;
		background: var(--border);
	}

	.tl-item {
		position: relative;
		padding-bottom: 2.75rem;
	}

	.tl-item:last-child {
		padding-bottom: 0;
	}

	.tl-marker {
		position: absolute;
		left: -3.5rem;
		top: 6px;
		width: 13px;
		height: 13px;
		border-radius: 50%;
		background: var(--white);
		border: 3px solid var(--border);
		transform: translateX(calc(6px - 50% + 1px));
		z-index: 1;
	}

	.tl-item.current .tl-marker {
		background: var(--red);
		border-color: var(--red);
		box-shadow: 0 0 0 5px rgba(237, 41, 57, 0.15);
	}

	.tl-year {
		display: block;
		font-size: 0.8125rem;
		font-weight: 700;
		color: var(--blue);
		text-transform: uppercase;
		letter-spacing: 0.06em;
		margin-bottom: 0.375rem;
		line-height: 1.3;
	}

	.tl-item h4 {
		font-size: 1.0625rem;
		color: var(--text);
		margin-bottom: 0.375rem;
		font-weight: 700;
		line-height: 1.4;
	}

	.tl-item p {
		font-size: 1rem;
		line-height: 1.85;
		color: #666;
	}

	/* ═══════════════════════════════════════
	   EVENTS
	   ═══════════════════════════════════════ */
	.events-section {
		background: var(--warm-white);
	}

	.events-placeholder {
		text-align: center;
		border: 2px dashed var(--border);
		border-radius: 12px;
		padding: 4rem 2rem;
		background: var(--light);
	}

	.coming-soon {
		font-size: 1.125rem;
		font-weight: 700;
		color: var(--blue);
		text-transform: uppercase;
		letter-spacing: 0.1em;
		margin-bottom: 0.75rem;
		line-height: 1.3;
	}

	.events-sub {
		font-size: 1rem;
		line-height: 1.85;
		color: #888;
		max-width: 400px;
		margin: 0 auto;
	}

	/* ═══════════════════════════════════════
	   CTA BOTTOM
	   ═══════════════════════════════════════ */
	.cta-bottom {
		background: var(--navy);
		padding: 6rem 0;
		text-align: center;
		color: var(--white);
		position: relative;
		z-index: 5;
	}

	.cta-top-accent {
		position: absolute;
		top: 0;
		left: 0;
		right: 0;
		height: 3px;
		background: linear-gradient(90deg, var(--blue), var(--red));
	}

	.cta-bottom-inner {
		max-width: 640px;
		margin: 0 auto;
		padding: 0 2rem;
	}

	.cta-bottom h2 {
		font-size: clamp(1.5rem, 3vw, 2.25rem);
		margin-bottom: 1rem;
		color: var(--white);
		line-height: 1.2;
		letter-spacing: -0.01em;
	}

	.cta-sub {
		font-size: 1.125rem;
		color: rgba(255, 255, 255, 0.6);
		margin-bottom: 2.5rem;
		line-height: 1.85;
	}

	.cta-bottom .buttons {
		display: flex;
		gap: 1rem;
		justify-content: center;
		flex-wrap: wrap;
	}

	/* ═══════════════════════════════════════
	   FOOTER
	   ═══════════════════════════════════════ */
	footer {
		text-align: center;
		padding: 2.5rem 2rem;
		font-size: 0.875rem;
		color: #999;
		border-top: 1px solid var(--border);
		display: flex;
		flex-direction: column;
		align-items: center;
		gap: 1rem;
		background: var(--white);
		line-height: 1.5;
	}

	.footer-logo {
		height: 36px;
		width: auto;
		opacity: 0.45;
	}

	/* ═══════════════════════════════════════
	   RESPONSIVE — 900px
	   ═══════════════════════════════════════ */
	@media (max-width: 900px) {
		.section-num {
			font-size: 2rem;
		}

		.section-title-sticky {
			font-size: 1rem;
		}

		.sticky-header-inner {
			gap: 1rem;
		}
	}

	/* ═══════════════════════════════════════
	   RESPONSIVE — 640px
	   ═══════════════════════════════════════ */
	@media (max-width: 640px) {
		/* Reduce sticky header height for mobile stacking */
		:root {
			--sticky-h: 60px;
		}

		/* Hero */
		.hero-inner {
			padding: 2.5rem 1.5rem 4rem;
		}

		.portrait {
			width: 128px;
			height: 128px;
		}

		.hero h1 {
			font-size: 2.25rem;
		}

		.role {
			font-size: 1rem;
		}

		.geo {
			font-size: 0.75rem;
			gap: 0.5rem;
		}

		.hero .quote {
			font-size: 1rem;
		}

		.facts {
			flex-direction: column;
			align-items: center;
			gap: 0.375rem;
		}

		.facts li {
			font-size: 0.8125rem;
		}

		.cta {
			font-size: 0.9375rem;
			padding: 0.8rem 1.75rem;
			width: 100%;
			text-align: center;
		}

		.hero .buttons,
		.cta-bottom .buttons {
			flex-direction: column;
			align-items: center;
		}

		/* Sticky headers — thinner on mobile */
		.sticky-header-inner {
			padding: 0.75rem 1.5rem;
			gap: 0.75rem;
		}

		.section-num {
			font-size: 1.375rem;
		}

		.section-label-sticky {
			font-size: 0.625rem;
		}

		.section-title-sticky {
			font-size: 0.875rem;
		}

		/* Section content */
		.section-content {
			padding: 2.5rem 1.5rem 4rem;
		}

		.priorities-content {
			gap: 1.25rem;
		}

		.card-inner {
			padding: 1.5rem 1.25rem;
		}

		.priority-card h3 {
			font-size: 1.125rem;
		}

		/* Quote band */
		.quote-band {
			padding: 3.5rem 1.5rem;
		}

		.quote-band blockquote::before,
		.quote-band blockquote::after {
			font-size: 2rem;
		}

		.quote-band blockquote {
			padding: 0 1.75rem;
		}

		/* Timeline */
		.timeline {
			padding-left: 2.75rem;
		}

		.tl-marker {
			left: -2.75rem;
		}

		.tl-item {
			padding-bottom: 2.25rem;
		}

		/* Events */
		.events-placeholder {
			padding: 3rem 1.5rem;
		}

		/* CTA bottom */
		.cta-bottom {
			padding: 4rem 0;
		}

		.cta-bottom-inner {
			padding: 0 1.5rem;
		}
	}
</style>
