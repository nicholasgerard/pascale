---
import Head from '../../components/Head.astro';
import '../../styles/fonts.css';
---

<html lang="fr">
	<head>
		<Head
			title="Pascale Richard — Ensemble pour NY 2026"
			description="Candidate Renaissance pour les Français de New York, New Jersey et Connecticut. Journaliste, élue, engagée depuis 2016."
			path="/v/3"
		/>
	</head>
	<body>

		<!-- ═══ HERO — Cinematic split: photo left, dark navy right ═══ -->
		<section class="split hero-split">
			<div class="panel hero-photo">
				<img src="/assets/images/pascale.jpg" alt="Pascale Richard" />
			</div>
			<div class="panel hero-content">
				<nav>
					<img src="/assets/logos/RENAISSANCE_logo-complet_blanc.png" alt="Renaissance" class="nav-logo" />
				</nav>

				<div class="hero-body">
					<h1>Pascale<br />Richard</h1>
					<p class="role">Conseillère des Français de l'étranger</p>
					<div class="geo">
						<span>New York</span>
						<span class="dot"></span>
						<span>New Jersey</span>
						<span class="dot"></span>
						<span>Connecticut</span>
					</div>

					<div class="gold-rule"></div>

					<p class="hero-quote">&laquo; Le plus satisfaisant est d'aider mes concitoyens. Conseiller des Français de l'étranger est une fonction méconnue, mais les conseillers font un travail important. &raquo;</p>

					<ul class="facts">
						<li><strong>25+ ans</strong> à New York</li>
						<li><strong>Engagée</strong> depuis 2016</li>
						<li><strong>Élue</strong> en 2021</li>
						<li><strong>Suppléante</strong> de R. Lescure</li>
					</ul>

					<div class="btn-row">
						<a href="https://www.instagram.com/pascalevrichard" target="_blank" rel="noopener noreferrer" class="btn btn-primary">Suivez-nous sur Instagram</a>
						<a href="https://renaissance-fde.fr/portrait-dune-elue-a-votre-service-pascale-richard-new-york/" target="_blank" rel="noopener noreferrer" class="btn btn-ghost">Découvrir Pascale</a>
					</div>
				</div>
			</div>
		</section>

		<!-- ═══ PRIORITIES — Left: navy + decorative numbers / Right: white + cards ═══ -->
		<section class="split priorities-split">
			<div class="panel priorities-deco">
				<span class="deco-num">01</span>
				<span class="deco-num">02</span>
				<span class="deco-num">03</span>
			</div>
			<div class="panel priorities-content">
				<p class="section-label">Engagements</p>
				<h2>Trois priorités pour notre communauté</h2>

				<div class="priority-stack">
					<div class="priority-card">
						<span class="card-num">01</span>
						<h3>Solidarité</h3>
						<p>Accompagner les Français en difficulté aux États-Unis — personnes âgées isolées, malades, familles touchées par la vie chère. Orienter chacun vers les bons interlocuteurs.</p>
					</div>
					<div class="priority-card">
						<span class="card-num">02</span>
						<h3>Lien &amp; événements</h3>
						<p>Soutenir les associations et événements qui créent du lien — de Bastille Day aux salons de l'éducation bilingue — et rassembler les Français de toute la circonscription.</p>
					</div>
					<div class="priority-card">
						<span class="card-num">03</span>
						<h3>Services numériques</h3>
						<p>Accompagner la mise en place de France Consulaire et le vote par Internet. Tenir les Français informés, simplifier les démarches administratives à distance.</p>
					</div>
				</div>
			</div>
		</section>

		<!-- ═══ QUOTE — Left: red + quote text / Right: blue + guillemets ═══ -->
		<section class="split quote-split">
			<div class="panel quote-red">
				<div class="quote-body">
					<div class="quote-mark">&laquo;</div>
					<blockquote>Il y a une résilience des gens qui n'attendent pas tout de leur gouvernement. C'est notre relation à l'État qui doit évoluer.</blockquote>
					<div class="quote-mark close">&raquo;</div>
				</div>
			</div>
			<div class="panel quote-blue">
				<span class="guillemet-giant">&laquo;&raquo;</span>
			</div>
		</section>

		<!-- ═══ PARCOURS — Left: white + timeline / Right: navy + vertical label ═══ -->
		<section class="split parcours-split">
			<div class="panel parcours-content">
				<p class="section-label">Parcours</p>
				<h2>D'une carrière dans les médias à l'engagement politique</h2>
				<div class="timeline">
					<div class="tl-item">
						<span class="tl-year">Années 2000</span>
						<div class="tl-body">
							<h4>Installation à New York</h4>
							<p>Carrière de journaliste. Rédactrice en chef de France Amérique. Directrice des événements culturels au Lycée Français de New York.</p>
						</div>
					</div>
					<div class="tl-item">
						<span class="tl-year">2016</span>
						<div class="tl-body">
							<h4>Engagement avec En Marche</h4>
							<p>S'engage dès la première heure avec le mouvement d'Emmanuel Macron. Campagne aux côtés de Roland Lescure pour l'Amérique du Nord.</p>
						</div>
					</div>
					<div class="tl-item">
						<span class="tl-year">2017</span>
						<div class="tl-body">
							<h4>Suppléante du député Lescure</h4>
							<p>Suppléante pendant la première mandature du député de la 1ère circonscription des Français établis hors de France.</p>
						</div>
					</div>
					<div class="tl-item">
						<span class="tl-year">2021</span>
						<div class="tl-body">
							<h4>Élue conseillère des Français de l'étranger</h4>
							<p>Tête de liste En Marche pour NY, NJ, CT et les Bermudes. Élue à l'Assemblée des Français de l'étranger, commission développement durable.</p>
						</div>
					</div>
					<div class="tl-item current">
						<span class="tl-year">2026</span>
						<div class="tl-body">
							<h4>Candidate aux législatives</h4>
							<p>Candidate Renaissance aux élections législatives pour représenter les Français de la circonscription tri-state.</p>
						</div>
					</div>
				</div>
			</div>
			<div class="panel parcours-deco">
				<span class="vertical-label">Parcours</span>
			</div>
		</section>

		<!-- ═══ EVENTS — Left: blue + 2026 deco / Right: white + content ═══ -->
		<section class="split events-split">
			<div class="panel events-deco">
				<div class="cal-deco">
					<span class="cal-year">2026</span>
					<span class="cal-icon">&#9783;</span>
				</div>
			</div>
			<div class="panel events-content">
				<p class="section-label">Événements</p>
				<h2>Rencontrons-nous</h2>
				<div class="events-placeholder">
					<p class="coming-soon">À venir</p>
					<p class="events-sub">Le calendrier des événements et rencontres sera bientôt disponible. Suivez-nous sur Instagram pour rester informés.</p>
				</div>
			</div>
		</section>

		<!-- ═══ CTA — Full-width dark (breaks split for emphasis) ═══ -->
		<section class="cta-section">
			<div class="cta-inner">
				<h2>Rejoignez le mouvement</h2>
				<p class="cta-sub">Ensemble, construisons l'avenir de notre communauté française aux États-Unis.</p>
				<div class="btn-row btn-row-center">
					<a href="https://www.instagram.com/pascalevrichard" target="_blank" rel="noopener noreferrer" class="btn btn-primary">Suivez-nous sur Instagram</a>
					<a href="https://renaissance-fde.fr/portrait-dune-elue-a-votre-service-pascale-richard-new-york/" target="_blank" rel="noopener noreferrer" class="btn btn-ghost">Découvrir Pascale</a>
				</div>
			</div>
		</section>

		<!-- ═══ FOOTER ═══ -->
		<footer>
			<div class="footer-tricolore"></div>
			<img src="/assets/logos/RENAISSANCE_monogramme_marine.png" alt="Renaissance" class="footer-logo" />
			<p>&copy; 2026 Pascale Richard &mdash; Ensemble pour NY 2026</p>
		</footer>

	</body>
</html>

<style>
	/* ─── PALETTE ─── */
	:root {
		--navy: #001B6E;
		--blue: #002395;
		--red: #ED2939;
		--white: #FFFFFF;
		--gold: #C9A84C;
		--offwhite: #F5F5FA;
		--text: #1A1A2E;
		--muted: #6B6B80;
		--border: #E0E0EC;
	}

	/* ─── RESET ─── */
	* { margin: 0; padding: 0; box-sizing: border-box; }

	html {
		font-family: 'Maax', system-ui, -apple-system, sans-serif;
		color: var(--text);
		scroll-behavior: smooth;
		font-size: 16px;
	}

	body {
		overflow-x: hidden;
		background: var(--white);
	}

	img { display: block; max-width: 100%; }

	/* ═══════════════════════════════════════════════
	   SPLIT GRID SYSTEM
	   Every section is a two-column 1fr 1fr grid.
	   Each .panel fills its half with generous padding.
	   ═══════════════════════════════════════════════ */
	.split {
		display: grid;
		grid-template-columns: 1fr 1fr;
		min-height: 0;
	}

	.panel {
		position: relative;
		overflow: hidden;
	}


	/* ═══════════════════════════════════════════════
	   HERO
	   Left: photo (object-fit cover, full height)
	   Right: navy background with content
	   ═══════════════════════════════════════════════ */
	.hero-split {
		min-height: 100vh;
	}

	.hero-photo {
		background: var(--navy);
	}

	.hero-photo img {
		width: 100%;
		height: 100%;
		object-fit: cover;
		object-position: center 20%;
	}

	.hero-content {
		background: var(--navy);
		color: var(--white);
		display: flex;
		flex-direction: column;
	}

	.hero-content nav {
		padding: 2.5rem 3.5rem 0;
	}

	.nav-logo {
		height: 22px;
		opacity: 0.7;
	}

	.hero-body {
		flex: 1;
		display: flex;
		flex-direction: column;
		justify-content: center;
		padding: 3rem 3.5rem 4rem;
	}

	.hero-body h1 {
		font-size: clamp(2.5rem, 4.5vw, 4rem);
		font-weight: 700;
		line-height: 1.05;
		margin-bottom: 0.75rem;
		letter-spacing: -0.02em;
	}

	.role {
		font-size: 1.125rem;
		color: rgba(255,255,255,0.65);
		margin-bottom: 0.625rem;
	}

	.geo {
		display: flex;
		align-items: center;
		gap: 0.75rem;
		font-size: 0.8125rem;
		color: rgba(255,255,255,0.4);
		text-transform: uppercase;
		letter-spacing: 0.12em;
		margin-bottom: 2.5rem;
	}

	.dot {
		width: 4px;
		height: 4px;
		border-radius: 50%;
		background: var(--gold);
	}

	.gold-rule {
		width: 48px;
		height: 3px;
		background: var(--gold);
		border-radius: 2px;
		margin-bottom: 2rem;
	}

	.hero-quote {
		font-size: 1.0625rem;
		font-style: italic;
		line-height: 1.75;
		color: rgba(255,255,255,0.7);
		margin-bottom: 2rem;
		max-width: 460px;
	}

	.facts {
		list-style: none;
		display: flex;
		flex-wrap: wrap;
		gap: 0.5rem 1.75rem;
		margin-bottom: 2.5rem;
	}

	.facts li {
		font-size: 0.875rem;
		color: rgba(255,255,255,0.45);
	}

	.facts strong {
		color: var(--gold);
	}


	/* ═══════════════════════════════════════════════
	   PRIORITIES
	   Left: navy with giant faded 01 02 03
	   Right: white/light with stacked cards
	   ═══════════════════════════════════════════════ */
	.priorities-deco {
		background: var(--navy);
		display: flex;
		flex-direction: column;
		align-items: center;
		justify-content: center;
		gap: 0;
		padding: 4rem 2rem;
	}

	.deco-num {
		font-size: clamp(7rem, 12vw, 11rem);
		font-weight: 700;
		color: var(--white);
		opacity: 0.06;
		line-height: 1.1;
		letter-spacing: -0.03em;
		user-select: none;
	}

	.priorities-content {
		background: var(--white);
		padding: 5rem 4rem;
		display: flex;
		flex-direction: column;
		justify-content: center;
	}

	.section-label {
		text-transform: uppercase;
		letter-spacing: 0.15em;
		font-size: 0.8125rem;
		font-weight: 700;
		color: var(--red);
		margin-bottom: 0.625rem;
	}

	.priorities-content h2 {
		font-size: clamp(1.5rem, 2.5vw, 2.125rem);
		color: var(--navy);
		margin-bottom: 2.5rem;
		line-height: 1.3;
	}

	.priority-stack {
		display: flex;
		flex-direction: column;
		gap: 1.25rem;
	}

	.priority-card {
		background: var(--offwhite);
		padding: 2rem 2rem 2rem 2rem;
		border-radius: 6px;
		border-left: 4px solid var(--blue);
		position: relative;
	}

	.card-num {
		display: inline-block;
		font-size: 0.75rem;
		font-weight: 700;
		color: var(--white);
		background: var(--red);
		padding: 0.2rem 0.6rem;
		border-radius: 3px;
		letter-spacing: 0.05em;
		margin-bottom: 0.75rem;
	}

	.priority-card h3 {
		font-size: 1.125rem;
		color: var(--navy);
		margin-bottom: 0.5rem;
		font-weight: 700;
	}

	.priority-card p {
		font-size: 0.9375rem;
		line-height: 1.7;
		color: var(--muted);
	}


	/* ═══════════════════════════════════════════════
	   QUOTE
	   Left: red with white text
	   Right: blue with giant guillemets
	   ═══════════════════════════════════════════════ */
	.quote-red {
		background: var(--red);
		display: flex;
		align-items: center;
		justify-content: center;
		padding: 5rem 4rem;
	}

	.quote-body {
		max-width: 460px;
	}

	.quote-mark {
		font-size: 3.5rem;
		font-weight: 700;
		color: var(--white);
		opacity: 0.4;
		line-height: 1;
		margin-bottom: 0.5rem;
	}

	.quote-mark.close {
		text-align: right;
		margin-bottom: 0;
		margin-top: 0.5rem;
	}

	.quote-body blockquote {
		font-size: clamp(1.125rem, 2vw, 1.5rem);
		font-style: italic;
		line-height: 1.7;
		color: var(--white);
		font-weight: 400;
	}

	.quote-blue {
		background: var(--blue);
		display: flex;
		align-items: center;
		justify-content: center;
	}

	.guillemet-giant {
		font-size: clamp(10rem, 16vw, 18rem);
		font-weight: 700;
		color: var(--white);
		opacity: 0.08;
		line-height: 1;
		letter-spacing: 0.1em;
		user-select: none;
	}


	/* ═══════════════════════════════════════════════
	   PARCOURS / TIMELINE
	   Left: white/light with timeline
	   Right: navy with vertical "PARCOURS" label
	   ═══════════════════════════════════════════════ */
	.parcours-content {
		background: var(--offwhite);
		padding: 5rem 3.5rem;
		display: flex;
		flex-direction: column;
		justify-content: center;
	}

	.parcours-content h2 {
		font-size: clamp(1.5rem, 2.5vw, 2rem);
		color: var(--navy);
		margin-bottom: 3rem;
		line-height: 1.3;
	}

	.parcours-deco {
		background: var(--navy);
		display: flex;
		align-items: center;
		justify-content: center;
	}

	.vertical-label {
		font-size: clamp(4rem, 8vw, 7.5rem);
		font-weight: 700;
		color: var(--white);
		opacity: 0.07;
		text-transform: uppercase;
		letter-spacing: 0.2em;
		writing-mode: vertical-lr;
		transform: rotate(180deg);
		user-select: none;
	}

	.timeline {
		position: relative;
		padding-left: 3rem;
	}

	.timeline::before {
		content: '';
		position: absolute;
		left: 6px;
		top: 6px;
		bottom: 6px;
		width: 2px;
		background: var(--border);
	}

	.tl-item {
		position: relative;
		padding-bottom: 2.25rem;
	}

	.tl-item:last-child {
		padding-bottom: 0;
	}

	.tl-item::before {
		content: '';
		position: absolute;
		left: -3rem;
		top: 6px;
		width: 12px;
		height: 12px;
		border-radius: 50%;
		background: var(--offwhite);
		border: 3px solid var(--border);
		transform: translateX(1px);
		z-index: 1;
	}

	.tl-item.current::before {
		background: var(--red);
		border-color: var(--red);
		box-shadow: 0 0 0 5px rgba(237,41,57,0.15);
	}

	.tl-year {
		display: block;
		font-size: 0.8125rem;
		font-weight: 700;
		color: var(--blue);
		text-transform: uppercase;
		letter-spacing: 0.06em;
		margin-bottom: 0.375rem;
	}

	.tl-body h4 {
		font-size: 1.0625rem;
		color: var(--text);
		margin-bottom: 0.375rem;
		font-weight: 700;
	}

	.tl-body p {
		font-size: 0.9375rem;
		line-height: 1.7;
		color: var(--muted);
	}


	/* ═══════════════════════════════════════════════
	   EVENTS
	   Left: blue with 2026 calendar deco
	   Right: white with placeholder content
	   ═══════════════════════════════════════════════ */
	.events-deco {
		background: var(--blue);
		display: flex;
		align-items: center;
		justify-content: center;
		padding: 4rem;
	}

	.cal-deco {
		display: flex;
		flex-direction: column;
		align-items: center;
		gap: 1rem;
		user-select: none;
	}

	.cal-year {
		font-size: clamp(5rem, 9vw, 8rem);
		font-weight: 700;
		color: var(--white);
		opacity: 0.1;
		line-height: 1;
		letter-spacing: 0.05em;
	}

	.cal-icon {
		font-size: clamp(3rem, 5vw, 4.5rem);
		color: var(--white);
		opacity: 0.08;
		line-height: 1;
	}

	.events-content {
		background: var(--white);
		padding: 5rem 4rem;
		display: flex;
		flex-direction: column;
		justify-content: center;
	}

	.events-content h2 {
		font-size: clamp(1.5rem, 2.5vw, 2rem);
		color: var(--navy);
		margin-bottom: 2.5rem;
		line-height: 1.3;
	}

	.events-placeholder {
		border: 2px dashed var(--border);
		border-radius: 10px;
		padding: 3rem 2rem;
		text-align: center;
	}

	.coming-soon {
		font-size: 1.125rem;
		font-weight: 700;
		color: var(--blue);
		text-transform: uppercase;
		letter-spacing: 0.1em;
		margin-bottom: 0.75rem;
	}

	.events-sub {
		font-size: 0.9375rem;
		line-height: 1.7;
		color: var(--muted);
		max-width: 360px;
		margin: 0 auto;
	}


	/* ═══════════════════════════════════════════════
	   CTA (full-width break)
	   ═══════════════════════════════════════════════ */
	.cta-section {
		background: var(--navy);
		padding: 6rem 2rem;
		text-align: center;
		color: var(--white);
	}

	.cta-inner {
		max-width: 640px;
		margin: 0 auto;
	}

	.cta-section h2 {
		font-size: clamp(1.5rem, 3vw, 2.25rem);
		margin-bottom: 1rem;
		color: var(--white);
	}

	.cta-sub {
		font-size: 1.125rem;
		color: rgba(255,255,255,0.55);
		margin-bottom: 2.5rem;
		line-height: 1.65;
	}


	/* ═══════════════════════════════════════════════
	   BUTTONS
	   ═══════════════════════════════════════════════ */
	.btn {
		display: inline-block;
		padding: 0.875rem 2.25rem;
		border-radius: 4px;
		font-weight: 700;
		font-size: 1rem;
		text-decoration: none;
		transition: background 0.2s, border-color 0.2s, transform 0.15s;
		cursor: pointer;
	}

	.btn-primary {
		background: var(--red);
		color: var(--white);
	}

	.btn-primary:hover {
		background: #c4212f;
		transform: translateY(-1px);
	}

	.btn-ghost {
		background: transparent;
		color: var(--white);
		border: 2px solid rgba(255,255,255,0.3);
	}

	.btn-ghost:hover {
		border-color: var(--white);
		background: rgba(255,255,255,0.08);
	}

	.btn-row {
		display: flex;
		gap: 1rem;
		flex-wrap: wrap;
	}

	.btn-row-center {
		justify-content: center;
	}


	/* ═══════════════════════════════════════════════
	   FOOTER
	   ═══════════════════════════════════════════════ */
	footer {
		text-align: center;
		padding: 0 2rem 3rem;
		font-size: 0.875rem;
		color: #999;
		display: flex;
		flex-direction: column;
		align-items: center;
		gap: 1rem;
	}

	.footer-tricolore {
		width: 100%;
		height: 4px;
		background: linear-gradient(90deg, var(--blue) 33.33%, var(--white) 33.33%, var(--white) 66.66%, var(--red) 66.66%);
		margin-bottom: 1.5rem;
	}

	.footer-logo {
		height: 36px;
		width: auto;
		opacity: 0.4;
	}


	/* ═══════════════════════════════════════════════
	   RESPONSIVE — below 768px
	   Splits collapse to single column.
	   Decorative panels shrink or hide.
	   ═══════════════════════════════════════════════ */
	@media (max-width: 768px) {
		/* Collapse all splits */
		.split {
			grid-template-columns: 1fr;
		}

		/* ── HERO ── */
		.hero-split {
			min-height: auto;
		}

		.hero-photo {
			min-height: 55vh;
			max-height: 65vh;
		}

		.hero-content nav {
			padding: 2rem 1.5rem 0;
		}

		.hero-body {
			padding: 2.5rem 1.5rem 3.5rem;
		}

		.hero-body h1 {
			font-size: 2.5rem;
		}

		.role { font-size: 1rem; }

		.geo {
			font-size: 0.75rem;
			gap: 0.5rem;
			margin-bottom: 2rem;
		}

		.hero-quote {
			font-size: 1rem;
		}

		.facts {
			flex-direction: column;
			gap: 0.375rem;
			margin-bottom: 2rem;
		}

		.facts li { font-size: 0.8125rem; }

		.btn-row {
			flex-direction: column;
		}

		.btn {
			width: 100%;
			text-align: center;
			padding: 0.85rem 1.5rem;
			font-size: 0.9375rem;
		}

		/* ── PRIORITIES ── */
		/* Deco panel: shrink to a compact bar */
		.priorities-deco {
			flex-direction: row;
			gap: 1.5rem;
			padding: 2rem 1.5rem;
			min-height: auto;
		}

		.deco-num {
			font-size: 4rem;
		}

		.priorities-content {
			padding: 3rem 1.5rem 4rem;
		}

		/* ── QUOTE ── */
		/* Hide the blue decorative panel on mobile */
		.quote-blue {
			display: none;
		}

		.quote-red {
			padding: 3.5rem 1.5rem;
		}

		.quote-body blockquote {
			font-size: 1.125rem;
		}

		/* ── PARCOURS ── */
		/* Show content first, deco after (shrunk) */
		.parcours-split {
			display: flex;
			flex-direction: column;
		}

		.parcours-content {
			padding: 3rem 1.5rem 4rem;
			order: 1;
		}

		.parcours-content h2 {
			margin-bottom: 2.5rem;
		}

		.parcours-deco {
			order: 2;
			min-height: 10vh;
			padding: 2rem;
		}

		.vertical-label {
			writing-mode: horizontal-tb;
			transform: none;
			font-size: 3rem;
		}

		.timeline {
			padding-left: 2.5rem;
		}

		.tl-item::before {
			left: -2.5rem;
		}

		.tl-item {
			padding-bottom: 1.75rem;
		}

		/* ── EVENTS ── */
		/* Deco first (shrunk), content second */
		.events-split {
			display: flex;
			flex-direction: column;
		}

		.events-deco {
			min-height: auto;
			padding: 2.5rem 1.5rem;
		}

		.cal-year {
			font-size: 4rem;
		}

		.cal-icon {
			font-size: 2.5rem;
		}

		.events-content {
			padding: 3rem 1.5rem 4rem;
		}

		/* ── CTA ── */
		.cta-section {
			padding: 4rem 1.5rem;
		}

		.cta-section .btn-row {
			flex-direction: column;
			align-items: center;
		}

		/* ── FOOTER ── */
		footer {
			padding: 0 1.5rem 2.5rem;
		}
	}
</style>
