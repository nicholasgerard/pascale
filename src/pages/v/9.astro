---
import Head from '../../components/Head.astro';
import '../../styles/fonts.css';
---

<html lang="fr">
	<head>
		<Head
			title="Pascale Richard — Ensemble pour NY 2026"
			description="Candidate Renaissance pour les Français de New York, New Jersey et Connecticut. Journaliste, élue, engagée depuis 2016."
			path="/v/9"
		/>
	</head>
	<body>

		<!-- ═══ HERO — Split: photo left, content right ═══ -->
		<section class="split hero-split">
			<div class="split-visual hero-photo">
				<img src="/assets/images/pascale.jpg" alt="Pascale Richard" />
			</div>
			<div class="split-text hero-content">
				<nav>
					<img src="/assets/logos/RENAISSANCE_logo-complet_blanc.png" alt="Renaissance" class="nav-logo" />
				</nav>

				<div class="hero-inner">
					<h1>Pascale Richard</h1>
					<p class="role">Conseillère des Français de l'étranger</p>
					<div class="geo">
						<span>New York</span>
						<span class="dot"></span>
						<span>New Jersey</span>
						<span class="dot"></span>
						<span>Connecticut</span>
					</div>

					<div class="divider"></div>

					<p class="quote">&laquo; Le plus satisfaisant est d'aider mes concitoyens. Conseiller des Français de l'étranger est une fonction méconnue, mais les conseillers font un travail important. &raquo;</p>

					<ul class="facts">
						<li><strong>25+ ans</strong> à New York</li>
						<li><strong>Engagée</strong> depuis 2016</li>
						<li><strong>Élue</strong> en 2021</li>
						<li><strong>Suppléante</strong> de R. Lescure</li>
					</ul>

					<div class="buttons">
						<a href="https://www.instagram.com/pascalevrichard" target="_blank" rel="noopener noreferrer" class="cta">Suivez-nous sur Instagram</a>
						<a href="https://renaissance-fde.fr/portrait-dune-elue-a-votre-service-pascale-richard-new-york/" target="_blank" rel="noopener noreferrer" class="cta ghost">Découvrir Pascale</a>
					</div>
				</div>
			</div>
		</section>

		<!-- ═══ PRIORITIES — Split: text left, numbers right ═══ -->
		<section class="split priorities-split">
			<div class="split-text priorities-content">
				<p class="section-label">Engagements</p>
				<h2>Trois priorités pour notre communauté</h2>

				<div class="priority-stack">
					<div class="priority-card">
						<div class="card-accent"></div>
						<div class="card-number">01</div>
						<h3>Solidarité</h3>
						<p>Accompagner les Français en difficulté aux États-Unis — personnes âgées isolées, malades, familles touchées par la vie chère. Orienter chacun vers les bons interlocuteurs.</p>
					</div>
					<div class="priority-card">
						<div class="card-accent"></div>
						<div class="card-number">02</div>
						<h3>Lien &amp; événements</h3>
						<p>Soutenir les associations et événements qui créent du lien — de Bastille Day aux salons de l'éducation bilingue — et rassembler les Français de toute la circonscription.</p>
					</div>
					<div class="priority-card">
						<div class="card-accent"></div>
						<div class="card-number">03</div>
						<h3>Services numériques</h3>
						<p>Accompagner la mise en place de France Consulaire et le vote par Internet. Tenir les Français informés, simplifier les démarches administratives à distance.</p>
					</div>
				</div>
			</div>
			<div class="split-visual priorities-numbers">
				<div class="big-numbers">
					<span>01</span>
					<span>02</span>
					<span>03</span>
				</div>
			</div>
		</section>

		<!-- ═══ QUOTE BAND — Split: red block left, quote right ═══ -->
		<section class="split quote-split">
			<div class="split-visual quote-red">
				<div class="red-deco">
					<div class="red-line"></div>
					<div class="red-line"></div>
					<div class="red-line"></div>
				</div>
			</div>
			<div class="split-text quote-content">
				<div class="quote-inner">
					<div class="quote-bar"></div>
					<blockquote>
						&laquo; Il y a une résilience des gens qui n'attendent pas tout de leur gouvernement. C'est notre relation à l'État qui doit évoluer. &raquo;
					</blockquote>
				</div>
			</div>
		</section>

		<!-- ═══ TIMELINE — Split: rotated label left, timeline right ═══ -->
		<section class="split parcours-split">
			<div class="split-visual parcours-label">
				<span class="rotated-text">Parcours</span>
			</div>
			<div class="split-text parcours-content">
				<p class="section-label">Parcours</p>
				<h2>D'une carrière dans les médias à l'engagement politique</h2>
				<div class="timeline">
					<div class="tl-item">
						<span class="tl-year">Années 2000</span>
						<div class="tl-content">
							<h4>Installation à New York</h4>
							<p>Carrière de journaliste. Rédactrice en chef de France Amérique. Directrice des événements culturels au Lycée Français de New York.</p>
						</div>
					</div>
					<div class="tl-item">
						<span class="tl-year">2016</span>
						<div class="tl-content">
							<h4>Engagement avec En Marche</h4>
							<p>S'engage dès la première heure avec le mouvement d'Emmanuel Macron. Campagne aux côtés de Roland Lescure pour l'Amérique du Nord.</p>
						</div>
					</div>
					<div class="tl-item">
						<span class="tl-year">2017</span>
						<div class="tl-content">
							<h4>Suppléante du député Lescure</h4>
							<p>Suppléante pendant la première mandature du député de la 1ère circonscription des Français établis hors de France.</p>
						</div>
					</div>
					<div class="tl-item">
						<span class="tl-year">2021</span>
						<div class="tl-content">
							<h4>Élue conseillère des Français de l'étranger</h4>
							<p>Tête de liste En Marche pour NY, NJ, CT et les Bermudes. Élue à l'Assemblée des Français de l'étranger, commission développement durable.</p>
						</div>
					</div>
					<div class="tl-item current">
						<span class="tl-year">2026</span>
						<div class="tl-content">
							<h4>Candidate aux législatives</h4>
							<p>Candidate Renaissance aux élections législatives pour représenter les Français de la circonscription tri-state.</p>
						</div>
					</div>
				</div>
			</div>
		</section>

		<!-- ═══ EVENTS — Split: content left, decorative right ═══ -->
		<section class="split events-split">
			<div class="split-text events-content">
				<p class="section-label">Événements</p>
				<h2>Rencontrons-nous</h2>
				<div class="events-placeholder">
					<p class="coming-soon-text">À venir</p>
					<p class="events-sub">Le calendrier des événements et rencontres sera bientôt disponible. Suivez-nous sur Instagram pour rester informé.</p>
				</div>
			</div>
			<div class="split-visual events-deco">
				<span class="big-label">À venir</span>
			</div>
		</section>

		<!-- ═══ CTA BOTTOM — Full width (breaks the split) ═══ -->
		<section class="cta-bottom">
			<div class="cta-inner">
				<h2>Rejoignez le mouvement</h2>
				<p class="cta-sub">Ensemble, construisons l'avenir de notre communauté française aux États-Unis.</p>
				<div class="buttons">
					<a href="https://www.instagram.com/pascalevrichard" target="_blank" rel="noopener noreferrer" class="cta">Suivez-nous sur Instagram</a>
					<a href="https://renaissance-fde.fr/portrait-dune-elue-a-votre-service-pascale-richard-new-york/" target="_blank" rel="noopener noreferrer" class="cta ghost-light">Découvrir Pascale</a>
				</div>
			</div>
		</section>

		<!-- ═══ FOOTER — Full width ═══ -->
		<footer>
			<img src="/assets/logos/RENAISSANCE_monogramme_marine.png" alt="Renaissance" class="footer-logo" />
			<p>&copy; 2026 Pascale Richard &mdash; Ensemble pour NY 2026</p>
		</footer>

	</body>
</html>

<style>
	/* ─── VARIABLES ─── */
	:root {
		--navy: #001B6E;
		--blue: #002395;
		--red: #ED2939;
		--white: #FFFFFF;
		--gold: #C9A84C;
		--text: #1A1A2E;
		--light: #F7F7FB;
		--border: #E4E4EC;
	}

	/* ─── RESET ─── */
	* { margin: 0; padding: 0; box-sizing: border-box; }

	html {
		font-family: 'Maax', system-ui, sans-serif;
		color: var(--text);
		scroll-behavior: smooth;
		font-size: 16px;
	}

	body {
		overflow-x: hidden;
	}

	/* ═══════════════════════════════════════
	   SPLIT SYSTEM — core two-panel grid
	   ═══════════════════════════════════════ */
	.split {
		display: grid;
		grid-template-columns: 1fr 1fr;
		min-height: 100vh;
	}

	.split-visual {
		position: relative;
		overflow: hidden;
	}

	.split-text {
		display: flex;
		flex-direction: column;
		justify-content: center;
	}


	/* ═══════════════════════════════════════
	   SECTION 1 — HERO
	   Left: portrait photo / Right: dark content
	   ═══════════════════════════════════════ */
	.hero-split {
		min-height: 100vh;
	}

	.hero-photo {
		background: var(--navy);
	}

	.hero-photo img {
		width: 100%;
		height: 100%;
		object-fit: cover;
		object-position: center top;
		display: block;
	}

	.hero-content {
		background: var(--navy);
		color: var(--white);
		padding: 0;
	}

	.hero-content nav {
		padding: 2.5rem 3rem 0;
	}

	.nav-logo {
		height: 22px;
		opacity: 0.7;
	}

	.hero-inner {
		padding: 3rem 4rem 4rem;
	}

	.hero-inner h1 {
		font-size: clamp(2.25rem, 4vw, 3.5rem);
		font-weight: 700;
		line-height: 1.08;
		margin-bottom: 0.625rem;
		color: var(--white);
	}

	.role {
		font-size: 1.125rem;
		color: rgba(255, 255, 255, 0.7);
		margin-bottom: 0.625rem;
	}

	.geo {
		display: flex;
		align-items: center;
		gap: 0.75rem;
		font-size: 0.875rem;
		color: rgba(255, 255, 255, 0.45);
		text-transform: uppercase;
		letter-spacing: 0.1em;
		margin-bottom: 2.5rem;
	}

	.dot {
		width: 4px;
		height: 4px;
		border-radius: 50%;
		background: var(--gold);
	}

	.divider {
		width: 50px;
		height: 3px;
		background: var(--gold);
		margin-bottom: 2rem;
		border-radius: 2px;
	}

	.quote {
		font-size: 1.0625rem;
		font-style: italic;
		line-height: 1.7;
		color: rgba(255, 255, 255, 0.75);
		margin-bottom: 2rem;
		max-width: 440px;
	}

	.facts {
		list-style: none;
		display: flex;
		flex-wrap: wrap;
		gap: 0.5rem 1.5rem;
		margin-bottom: 2.5rem;
	}

	.facts li {
		font-size: 0.875rem;
		color: rgba(255, 255, 255, 0.5);
	}

	.facts strong {
		color: var(--gold);
	}

	.hero-content .buttons {
		display: flex;
		gap: 1rem;
		flex-wrap: wrap;
	}


	/* ═══════════════════════════════════════
	   SECTION 2 — PRIORITIES
	   Left: text / Right: big numbers on blue
	   ═══════════════════════════════════════ */
	.priorities-split {
		min-height: auto;
	}

	.priorities-content {
		padding: 5rem 4rem;
		background: var(--white);
	}

	.section-label {
		text-transform: uppercase;
		letter-spacing: 0.15em;
		font-size: 0.8125rem;
		font-weight: 700;
		color: var(--red);
		margin-bottom: 0.625rem;
	}

	.priorities-content h2 {
		font-size: clamp(1.5rem, 2.5vw, 2rem);
		color: var(--blue);
		margin-bottom: 2.5rem;
		line-height: 1.3;
	}

	.priority-stack {
		display: flex;
		flex-direction: column;
		gap: 1.5rem;
	}

	.priority-card {
		position: relative;
		background: var(--light);
		padding: 2rem 1.75rem;
		border-radius: 8px;
		overflow: hidden;
	}

	.card-accent {
		position: absolute;
		top: 0;
		left: 0;
		width: 100%;
		height: 3px;
		background: linear-gradient(90deg, var(--blue), var(--red));
	}

	.card-number {
		font-size: 0.8125rem;
		font-weight: 700;
		color: var(--red);
		margin-bottom: 0.75rem;
		letter-spacing: 0.05em;
	}

	.priority-card h3 {
		font-size: 1.125rem;
		color: var(--blue);
		margin-bottom: 0.5rem;
	}

	.priority-card p {
		font-size: 0.9375rem;
		line-height: 1.7;
		color: #555;
	}

	.priorities-numbers {
		background: var(--blue);
		display: flex;
		align-items: center;
		justify-content: center;
	}

	.big-numbers {
		display: flex;
		flex-direction: column;
		gap: 1rem;
		padding: 3rem;
	}

	.big-numbers span {
		font-size: clamp(5rem, 10vw, 9rem);
		font-weight: 700;
		color: var(--white);
		opacity: 0.08;
		line-height: 1;
		letter-spacing: -0.03em;
	}


	/* ═══════════════════════════════════════
	   SECTION 3 — QUOTE BAND
	   Left: solid red / Right: white with quote
	   ═══════════════════════════════════════ */
	.quote-split {
		min-height: 50vh;
	}

	.quote-red {
		background: var(--red);
		display: flex;
		align-items: center;
		justify-content: center;
	}

	.red-deco {
		display: flex;
		flex-direction: column;
		gap: 12px;
		padding: 3rem;
	}

	.red-line {
		width: 80px;
		height: 3px;
		background: var(--white);
		opacity: 0.25;
		border-radius: 2px;
	}

	.red-line:nth-child(2) {
		width: 120px;
		opacity: 0.15;
	}

	.red-line:nth-child(3) {
		width: 60px;
		opacity: 0.35;
	}

	.quote-content {
		background: var(--white);
		display: flex;
		align-items: center;
		justify-content: center;
		padding: 4rem;
	}

	.quote-inner {
		max-width: 480px;
	}

	.quote-bar {
		width: 40px;
		height: 3px;
		background: var(--gold);
		margin-bottom: 1.5rem;
		border-radius: 2px;
	}

	.quote-inner blockquote {
		font-size: clamp(1.125rem, 2vw, 1.375rem);
		font-style: italic;
		line-height: 1.7;
		color: var(--text);
	}


	/* ═══════════════════════════════════════
	   SECTION 4 — TIMELINE
	   Left: navy with rotated PARCOURS / Right: timeline
	   ═══════════════════════════════════════ */
	.parcours-split {
		min-height: auto;
	}

	.parcours-label {
		background: var(--navy);
		display: flex;
		align-items: center;
		justify-content: center;
	}

	.rotated-text {
		font-size: clamp(4rem, 8vw, 7rem);
		font-weight: 700;
		color: var(--white);
		opacity: 0.06;
		text-transform: uppercase;
		letter-spacing: 0.15em;
		writing-mode: vertical-lr;
		transform: rotate(180deg);
	}

	.parcours-content {
		padding: 5rem 4rem;
		background: var(--light);
	}

	.parcours-content .section-label {
		margin-bottom: 0.625rem;
	}

	.parcours-content h2 {
		font-size: clamp(1.5rem, 2.5vw, 2rem);
		color: var(--blue);
		margin-bottom: 3rem;
		line-height: 1.3;
	}

	.timeline {
		position: relative;
		padding-left: 3.5rem;
	}

	.timeline::before {
		content: '';
		position: absolute;
		left: 0.5625rem;
		top: 0.375rem;
		bottom: 0.375rem;
		width: 2px;
		background: var(--border);
	}

	.tl-item {
		position: relative;
		padding-bottom: 2.5rem;
	}

	.tl-item:last-child {
		padding-bottom: 0;
	}

	.tl-item::before {
		content: '';
		position: absolute;
		left: -3.5rem;
		top: 0.375rem;
		width: 13px;
		height: 13px;
		border-radius: 50%;
		background: var(--white);
		border: 3px solid var(--border);
		transform: translateX(calc(0.5625rem - 50% + 1px));
		z-index: 1;
	}

	.tl-item.current::before {
		background: var(--red);
		border-color: var(--red);
		box-shadow: 0 0 0 5px rgba(237, 41, 57, 0.15);
	}

	.tl-year {
		display: block;
		font-size: 0.8125rem;
		font-weight: 700;
		color: var(--blue);
		text-transform: uppercase;
		letter-spacing: 0.06em;
		margin-bottom: 0.375rem;
	}

	.tl-content h4 {
		font-size: 1.0625rem;
		color: var(--text);
		margin-bottom: 0.375rem;
		font-weight: 700;
	}

	.tl-content p {
		font-size: 0.9375rem;
		line-height: 1.7;
		color: #666;
	}


	/* ═══════════════════════════════════════
	   SECTION 5 — EVENTS
	   Left: white content / Right: blue with label
	   ═══════════════════════════════════════ */
	.events-split {
		min-height: 60vh;
	}

	.events-content {
		padding: 5rem 4rem;
		background: var(--white);
	}

	.events-content h2 {
		font-size: clamp(1.5rem, 2.5vw, 2rem);
		color: var(--blue);
		margin-bottom: 2.5rem;
		line-height: 1.3;
	}

	.events-placeholder {
		border: 2px dashed var(--border);
		border-radius: 10px;
		padding: 3rem 2rem;
		text-align: center;
	}

	.coming-soon-text {
		font-size: 1.125rem;
		font-weight: 700;
		color: var(--blue);
		text-transform: uppercase;
		letter-spacing: 0.1em;
		margin-bottom: 0.75rem;
	}

	.events-sub {
		font-size: 0.9375rem;
		line-height: 1.7;
		color: #888;
		max-width: 360px;
		margin: 0 auto;
	}

	.events-deco {
		background: var(--blue);
		display: flex;
		align-items: center;
		justify-content: center;
	}

	.big-label {
		font-size: clamp(3rem, 6vw, 5rem);
		font-weight: 700;
		color: var(--white);
		opacity: 0.07;
		text-transform: uppercase;
		letter-spacing: 0.1em;
	}


	/* ═══════════════════════════════════════
	   SECTION 6 — CTA BOTTOM (full width)
	   ═══════════════════════════════════════ */
	.cta-bottom {
		background: var(--navy);
		padding: 6rem 2rem;
		text-align: center;
		color: var(--white);
	}

	.cta-inner {
		max-width: 640px;
		margin: 0 auto;
	}

	.cta-bottom h2 {
		font-size: clamp(1.5rem, 3vw, 2.25rem);
		margin-bottom: 1rem;
		color: var(--white);
	}

	.cta-sub {
		font-size: 1.125rem;
		color: rgba(255, 255, 255, 0.6);
		margin-bottom: 2.5rem;
		line-height: 1.6;
	}

	.cta-bottom .buttons {
		display: flex;
		gap: 1rem;
		justify-content: center;
		flex-wrap: wrap;
	}


	/* ═══════════════════════════════════════
	   BUTTONS (shared)
	   ═══════════════════════════════════════ */
	.cta {
		display: inline-block;
		padding: 0.875rem 2.25rem;
		border-radius: 4px;
		font-weight: 700;
		font-size: 1rem;
		text-decoration: none;
		transition: all 0.2s;
		background: var(--red);
		color: var(--white);
	}

	.cta:hover {
		background: #c4212f;
	}

	.cta.ghost {
		background: transparent;
		color: var(--white);
		border: 2px solid rgba(255, 255, 255, 0.3);
	}

	.cta.ghost:hover {
		border-color: var(--white);
		background: rgba(255, 255, 255, 0.08);
	}

	.cta.ghost-light {
		background: transparent;
		color: var(--white);
		border: 2px solid rgba(255, 255, 255, 0.35);
	}

	.cta.ghost-light:hover {
		border-color: var(--white);
		background: rgba(255, 255, 255, 0.1);
	}


	/* ═══════════════════════════════════════
	   FOOTER (full width)
	   ═══════════════════════════════════════ */
	footer {
		text-align: center;
		padding: 2.5rem 2rem;
		font-size: 0.875rem;
		color: #999;
		border-top: 1px solid #eee;
		display: flex;
		flex-direction: column;
		align-items: center;
		gap: 1rem;
	}

	.footer-logo {
		height: 36px;
		width: auto;
		opacity: 0.45;
	}


	/* ═══════════════════════════════════════
	   RESPONSIVE — Collapse to single column
	   ═══════════════════════════════════════ */
	@media (max-width: 768px) {
		.split {
			grid-template-columns: 1fr;
			min-height: auto;
		}

		/* Hero: photo on top, content below */
		.hero-photo {
			min-height: 50vh;
			max-height: 60vh;
		}

		.hero-content nav {
			padding: 2rem 1.5rem 0;
		}

		.hero-inner {
			padding: 2.5rem 1.5rem 3.5rem;
		}

		.hero-inner h1 {
			font-size: 2.25rem;
		}

		.role {
			font-size: 1rem;
		}

		.geo {
			font-size: 0.75rem;
			gap: 0.5rem;
		}

		.quote {
			font-size: 1rem;
		}

		.facts {
			flex-direction: column;
			gap: 0.375rem;
			margin-bottom: 2rem;
		}

		.facts li {
			font-size: 0.8125rem;
		}

		.hero-content .buttons {
			flex-direction: column;
		}

		.cta {
			font-size: 0.9375rem;
			padding: 0.8rem 1.75rem;
			width: 100%;
			text-align: center;
		}

		/* Priorities: stack vertically, numbers on top */
		.priorities-split {
			display: flex;
			flex-direction: column-reverse;
		}

		.priorities-content {
			padding: 3rem 1.5rem 4rem;
		}

		.priorities-numbers {
			padding: 3rem 1.5rem;
			min-height: auto;
		}

		.big-numbers {
			flex-direction: row;
			gap: 1.5rem;
			padding: 2rem;
		}

		.big-numbers span {
			font-size: 4rem;
		}

		/* Quote: red on top, quote below */
		.quote-split {
			min-height: auto;
		}

		.quote-red {
			min-height: 20vh;
		}

		.quote-content {
			padding: 3rem 1.5rem;
		}

		/* Timeline: label on top, timeline below */
		.parcours-label {
			min-height: 15vh;
			padding: 2rem;
		}

		.rotated-text {
			writing-mode: horizontal-tb;
			transform: none;
			font-size: 3rem;
		}

		.parcours-content {
			padding: 3rem 1.5rem 4rem;
		}

		.parcours-content h2 {
			margin-bottom: 2.5rem;
		}

		.timeline {
			padding-left: 2.75rem;
		}

		.tl-item::before {
			left: -2.75rem;
		}

		.tl-item {
			padding-bottom: 2rem;
		}

		/* Events: content on top, deco below */
		.events-split {
			min-height: auto;
			display: flex;
			flex-direction: column;
		}

		.events-content {
			padding: 3rem 1.5rem 4rem;
		}

		.events-deco {
			min-height: 20vh;
		}

		/* CTA */
		.cta-bottom {
			padding: 4rem 1.5rem;
		}

		.cta-bottom .buttons {
			flex-direction: column;
			align-items: center;
		}
	}
</style>
